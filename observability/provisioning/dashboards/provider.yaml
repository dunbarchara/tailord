apiVersion: 1

providers:
  - name: tailord
    orgId: 1
    folder: Tailord
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
