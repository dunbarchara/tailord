apiVersion: 1

providers:
  - name: tailord
    orgId: 1
    folder: Tailord
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    allowUiUpdates: true   # set false after initial build to enforce JSON as source of truth
    options:
      path: /var/lib/grafana/dashboards
