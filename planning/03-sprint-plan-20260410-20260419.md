# Sprint Plan — April 10–19, 2026

*10 days. Source: `02-sprint-20260410-20260417.md`.*

**Scope rationale:** Epics 4 (B2B/API platform), 3.2 (interactive tailoring), and 3.3 (profile chat) are intentionally excluded — they require real users or prerequisites not yet met. Epic 1.3 scoring architecture improvements require the eval pipeline (Day 10) as a measurement baseline before committing. Everything included here either ships user-visible value, closes platform risk, or lays the foundation for the next sprint's LLM quality work.

---

## Day 1 — Cleanup & Quick Wins ✅

**Theme:** Zero maintenance debt. Close the gap between what the codebase says it does and what it actually does.

- [x] ~~Remove legacy backend endpoints: `router` files for `/parse`, `/generate`, `/job` (`job.py`)~~ — already removed in a prior session; `main.py` only includes `experience`, `notion`, `tailorings`, `users`.
- [x] Remove dead frontend routes (audit and delete) — audited all routes:
  - `(mock)/mock/job-software-engineer/` — **kept**, used for dev/testing, more mocks planned
  - `(auth)/checking/` — **live**, polls session status every 3s and routes to `/dashboard` or `/pending`
  - `(auth)/pending/` — **live**, shown to unapproved users awaiting access
  - `app/privacy/` and `app/terms/` — moved to `(marketing)/privacy/` and `(marketing)/terms/` to match routing architecture in CLAUDE.md
- [x] ~~**Design token:** `--color-text-link` → `#16A34A` in `globals.css`~~ — **not applicable**; the green-accent-for-links decision was reverted in favor of the charcoal/cream scheme. `--color-text-link` is correctly `#2A2A2A` (light) / `#E8E6E1` (dark).
- [x] ~~**GitHub UI:** Branch protection rule on `main`~~ — **not applicable**; CI job checks on PRs already enforced and branch push/delete protection is in place. GitHub branch protection rules don't apply to private repos on the free tier; will be enforced automatically when the repo goes public.

**Deliverable:** Codebase route audit complete. `npm run lint` + `ruff check` stay clean.

---

## Day 2 — Staging Bootstrap Completion ✅

**Theme:** The staging environment exists in Terraform but is not fully live. Close this out so CI deploys to a real staging environment.

- [x] Set up `staging-azure` GitHub environment with OIDC secrets
- [x] Run `terraform apply` with full variable set
- [x] Execute PostgreSQL user bootstrap SQL
- [x] Bind TLS certs: `tailord.app` and `staging.tailord.app`
- [x] Restore Cloudflare CNAMEs to proxied after cert binding
- [x] Trigger first CI deploy — both environments running real app images
- [x] Smoke test: staging frontend → staging backend confirmed; prod unaffected

**Deliverable:** CI pipeline deploys to staging on every PR merge. `staging.tailord.app` is live and isolated from prod. ✅ Complete.

---

## Day 3 — Privacy, Legal & Notion Review Prep ✅ (partial)

**Theme:** Ship the legal pages that unlock Notion's public integration review. Required before Tailord can be used by more than 10 Notion workspaces.

- [x] `(marketing)/privacy/page.tsx` — Privacy policy generated with TermsFeed; already live.
- [x] `(marketing)/terms/page.tsx` — Terms of use generated with Termly; already live.
- [~] Add Privacy + Terms links to `/u/{slug}/{tailoringSlug}` footer — **deferred**. The tailoring page already links back to the Tailord homepage, which has footer links to both pages. Direct footer links on the tailoring page are best practice for Notion's review checklist (privacy/terms discoverable from any surface displaying user data), but the current approach is arguably sufficient. Revisit when Notion flags it during review or when real users arrive.
- [x] Notion integration created with 256×256px logo/icon and integration description.
- [~] Demo screenshots — **deferred** until the product UI is more polished.
- [~] Submit Tailord for Notion public integration review — **deferred**. Will submit once screenshots are ready and there is enough usage to justify unlocking beyond 10 workspaces.

**Deliverable:** `tailord.app/privacy` and `tailord.app/terms` are live. Notion submission sent.

---

## Day 4 — Internal Admin Page ✅

**Theme:** Founders can approve, view, and manage users without DB access. Replaces `dev_approve.py` for staging/prod.

**Security design:** Admin access is tied to `is_admin: bool` on the `User` model, not a shared secret env var. Protection layers: Google OAuth (with MFA) → NextAuth JWT (encrypted JWE, tamper-proof) → `proxy.ts` (UX gate, checks `token.isAdmin`) → backend `require_admin()` dependency (always reads from DB — never trusts client claims). Backend is internal ingress only; `X-User-Id` header is injected server-side by the Next.js proxy, never by the client.

**Backend:**
- [x] `is_admin: bool` column added to `User` model; Alembic migration `c9e1f2a3b4d5`
- [x] `require_admin()` FastAPI dependency in `deps_user.py` — DB read, raises 403 if `user.is_admin` is false; logs all rejections
- [x] `api/admin.py` — `GET /admin/users`, `POST /admin/users/{id}/approve`, `POST /admin/users/{id}/revoke`; admin accounts are unrevokable via the UI (enforced in the revoke endpoint)
- [x] Registered in `main.py`
- [x] **Manual Azure step:** Bootstrap SQL run — founder account(s) set to `is_admin = true, status = 'approved'` via psql. Documented in `BOOTSTRAP.md` step 7.

**Frontend:**
- [x] `is_admin` → `token.isAdmin` in `auth.ts` jwt callback; exposed as `session.user.isAdmin`; type-safe via `next-auth.d.ts`
- [x] `proxy.ts` — `/admin` and `/admin/:path*` added to matcher; non-admins redirected to `/dashboard`
- [x] `/api/admin/users` GET, `/api/admin/users/[id]/approve` POST, `/api/admin/users/[id]/revoke` POST — all validate `session.user.isAdmin` server-side before proxying
- [x] `/admin/page.tsx` + `/admin/AdminView.tsx` — refactored to server component pattern (matching `dashboard/page.tsx` → `DashboardHome` split): `page.tsx` is an async server component that validates `isAdmin` and fetches users before any HTML is sent; `AdminView.tsx` is the client component for interactivity, uses `router.refresh()` post-mutation instead of client-side refetch
- [x] `components/ui/icons.tsx` — `IconCheck` and `IconWorkflows` extracted from 4 and 2 duplicate inline definitions respectively; all callsites updated (`AdminView`, `DashboardHome`, `ExperienceManager`, `SettingsPanel`, `Sidebar`)
- [x] `next.config.ts` — `remotePatterns` added for `lh3.googleusercontent.com` (Google avatar images via `<Image />`)

**Post-refactor status on Gemini's review points:**
1. **Security / server components** ✅ — `page.tsx` is async server component; `getServerSession` + `redirect` runs before any HTML is sent; data fetch is server-side. Non-admins get a 307 redirect, never a loading state.
2. **Error handling / race conditions** ✅ — `useEffect` fetch pattern eliminated; no concurrent fetch to cancel; `actionInProgress` state is the only async concern in `AdminView`.
3. **eslint-disable** ✅ — `useEffect` + `setLoading` pattern gone entirely; `AdminView` has no `useEffect`; no linter suppressions needed.
4. **Layout shift** ✅ — data arrives with the initial server render; `router.refresh()` re-runs the server fetch and streams updated HTML.

**Deliverable:** Admin page live at `/admin`. `dev_approve.py` retired for staging/prod — only needed for local dev.

**Future work:** Cloudflare Access can be placed in front of `/admin` at the network layer (zero code changes) for an additional "defense in depth" gate if warranted.

**Excerpt from security conversation:** If backend ingress wasn't restricted, the standard defenses are:

- Request signing (HMAC) — instead of a static X-API-Key, each request is signed with a timestamp + nonce + payload hash. Backend verifies the signature and rejects anything older than ~30s. A leaked key alone isn't enough — you'd also need to forge the signature on each new request.
- Mutual TLS (mTLS) — backend requires a client certificate from the frontend container. Only the frontend (holding the cert) can establish a connection. Common in service meshes (Istio, Linkerd). Completely eliminates the header-spoofing surface.
- Short-lived service JWTs — frontend mints a JWT signed with a private key, backend verifies with the public key. The user claims (including user ID) are inside a tamper-proof envelope, not a plain header.
- VNet-level controls — private endpoints, subnet restrictions, so the backend is simply unreachable outside the VNet regardless of what credentials you hold.

For your Azure setup, VNet integration + private endpoints for the backend container is the natural upgrade path when that becomes a compliance requirement. It's noted as deferred in CLAUDE.md already.

---

## Day 5 — LLM Telemetry — Profile Snapshot (Level 2)

**Theme:** Make the debug tab accurate. Currently it reconstructs the formatted profile from the *current* experience, which may have changed since the tailoring was generated. A snapshot makes debugging trustworthy.

- [x] Add `profile_snapshot` nullable `Text` column to `Tailoring` model
- [x] Alembic migration `b2c3d4e5f6a7` (also includes `tailoring_debug_logs` table)
- [x] Populate `profile_snapshot` in `_finalize_tailoring()` — stored before generation begins so the snapshot persists even on generation failure; overwrites on regen
- [x] Update `GET /tailorings/{id}/debug-info` response — returns snapshot if present, falls back to reconstruction for pre-snapshot tailorings; adds `profile_snapshot_source: "snapshot" | "reconstructed"` field
- [x] `DebugPanel.tsx` — "Formatted Profile" section shows provenance badge: green "Snapshot from generation" or amber "Reconstructed — experience may have changed"
- [x] `tailoring_debug_logs` table schema + migration *(schema only — no data written yet; this is Level 3 scaffolding for the next sprint)*

**Also completed (pre-requisites surfaced during Day 5 prep):**
- [x] `ExperienceManager.tsx` — fixed `handleRemove` silently swallowing errors; added confirm dialogs for resume remove, resume replace, GitHub remove, and GitHub username change
- [x] `app/api/experience.py` — removed `uploaded_at = None` from `_clear_resume_fields()` (column is NOT NULL); made `uploaded_at` nullable via migration `a1b2c3d4e5f6` as the correct fix
- [x] `app/prompts/tailoring.py` — replaced hardcoded name `Charles` in gap handling example with `[FIRST_NAME]` placeholder (user updated to `[Candidate]`)
- [x] `app/api/tailorings.py` — fixed `AttributeError` in reconstructed path when `extracted_profile` is `None`

**Deliverable:** Debug panel shows the exact profile the LLM saw, not a potentially-stale reconstruction.

---

## Day 6 — GitHub Deep Crawl (Backend)

**Theme:** Move GitHub from surface-level metadata to genuine experience signal. A repo with a detailed README and a `package.json` showing React + PostgreSQL + Docker should score very differently from a repo with just a name.

- [ ] `GET /experience/github/{username}/repos/enrich` — or integrate into existing `POST /experience/github` flow as a background task
- [ ] GitHub Contents API helper — for each repo, attempt to fetch (in order): `README.md`, `CLAUDE.md`, `package.json`, `pyproject.toml`, `go.mod`, `Dockerfile`, first `.github/workflows/*.yml` match
- [ ] LLM enrichment pass — single call per repo: `readme_summary` (2–3 sentences) + `detected_stack` (specific frameworks/tools, not just language)
- [ ] DB schema — `github_repo_details` JSON column on `experiences` (additive; backward-compatible with existing `github_repos` metadata)
- [ ] Alembic migration
- [ ] Respect rate limits: cap at top 20 repos by `pushed_at`; skip archived and repos inactive > 2 years; log request count

**Deliverable:** When a user connects GitHub, the enrichment background task crawls file content and stores structured per-repo summaries + detected stacks.

---

## Day 7 — GitHub Deep Crawl (Frontend) + Race Condition Fix

**Theme:** Surface the enriched GitHub data in the UI, and fix the long-standing race condition in experience processing.

**GitHub frontend:**
- [ ] `ParsedProfile` GitHub tab — replace flat repo list (name, language, stars) with enriched cards: detected stack badges, readme summary excerpt, notable files found
- [ ] Trigger re-enrichment UI — "Re-scan GitHub" button if `github_repo_details` is absent or stale

**Race condition fix:**
- [ ] `POST /experience/github` — do not set `status → "ready"` if `status` is currently `"processing"`; only set to ready if status is `"pending"` or `"ready"`
- [ ] `experience_processor.py` — update only `extracted_profile`, `status`, `processed_at` columns; use a targeted `UPDATE` rather than full record save to avoid clobbering concurrent GitHub writes

**Homepage:**
- [ ] `ProductPreview` — replace stylized mockup with real screenshot of the Fit Analysis view (Analysis tab is now polished; capture a clean screenshot and swap it in)

**Deliverable:** GitHub profile enrichment is visible to users. Race condition eliminated. Homepage shows the real product.

---

## Day 8 — Post-Tailoring Gap Detection (Backend)

**Theme:** After a tailoring is generated, the platform knows both the job requirements and the candidate's profile. Use that to surface targeted follow-up questions — specific to this job, specific to this candidate.

- [ ] `app/prompts/gap_analysis.py` — system prompt + user template; task: identify requirements not sourceable from profile, generate specific follow-up questions (not generic)
- [ ] `ProfileGap` + `GapAnalysis` Pydantic schemas in `app/schemas/`
  ```python
  class ProfileGap(BaseModel):
      job_requirement: str
      question_for_candidate: str
      context: str           # why this matters for this specific role
      source_searched: str   # what source was checked

  class GapAnalysis(BaseModel):
      gaps: list[ProfileGap]
      sourced_claim_count: int
      unsourced_claim_count: int
  ```
- [ ] `app/services/gap_analyzer.py` — calls `llm_parse_with_retry`; runs after `_finalize_tailoring` as a chained background task
- [ ] Store gap analysis as `gap_analysis` JSON column on `Tailoring` (nullable); Alembic migration
- [ ] `GET /tailorings/{id}` response — include `gap_analysis` field
- [ ] `POST /tailorings/{id}/gap-answer` — accepts `{question_index, answer}`, appends answer to `experience.user_input_text`; triggers re-enrichment

**Deliverable:** Backend detects and stores gap analysis for every newly-generated tailoring.

---

## Day 9 — Gap Detection (Frontend) + Claude Code Workflow

**Theme:** Surface gap questions in the UI, and set up the developer workflow improvements that pay dividends across every future session.

**Gap detection frontend:**
- [ ] `TailoringDetail.tsx` — below the generated tailoring (in the Analysis tab, after the FitAnalysis component): collapsible "Strengthen your profile" section; only shown when `gap_analysis.gaps.length > 0`
- [ ] `GapQuestions` component — card per gap: `job_requirement` as heading, `question_for_candidate` as the prompt, `context` as secondary text, inline textarea + Save answer button
- [ ] On answer save: POST to `/api/tailorings/{id}/gap-answer`; show "Saved — your experience has been updated" confirmation with link to Experience page

**Claude Code workflow:**
- [ ] Path-specific rules — `.claude/rules/frontend.md` (`paths: "frontend/**"`), `backend.md` (`paths: "backend/**"`), `api-contracts.md` (`paths: "frontend/src/app/api/**"`)
- [ ] Post-edit hooks in `.claude/settings.local.json` — after frontend Edit: `tsc --noEmit`; after backend Edit: `ruff check` on changed file
- [ ] Skills: `new-api-route` and `new-backend-endpoint` in `.claude/skills/` — most frequently repeated task patterns

**Deliverable:** Users see targeted gap questions after tailoring generation and can answer inline. Claude Code sessions are faster and catch errors earlier.

---

## Day 10 — Test Coverage + Eval Foundation

**Theme:** Close the biggest platform risk (42% test coverage) and lay the measurement foundation that all future LLM quality work depends on.

**Test coverage:**
- [ ] Push backend coverage: `app/api/tailorings.py` from 39% toward 70%+ — focus on CRUD paths, share/unshare, admin endpoints added this sprint
- [ ] Notion export API integration tests — mock `requests.Session` calls with `responses` library
- [ ] Rate limit soft warning — warn at 8 LLM triggers/hour (currently hard blocks at 10 with no warning); add warning field to `GET /tailorings` response or emit as SSE stage event

**Eval pipeline foundation (Level 4 skeleton):**
- [ ] Create `backend/tests/eval/` directory with `README.md` explaining the eval approach
- [ ] Build initial test set: 3–5 `(job_url, formatted_profile, expected_scores[])` fixtures covering distinct patterns — strong YOE match, partial skill match, clear gap, non-evaluable section, multi-source profile
- [ ] `eval_runner.py` skeleton — loads fixtures, runs `enrich_job_chunks` against each, computes agreement rate (exact match); prints summary report
- [ ] Not yet in CI — manual run only; the goal is to establish a baseline number before any prompt changes

**Portfolio write-up:**
- [ ] `planning/12-portfolio-writeup.md` — the problem Tailord solves, key technical decisions (dual pipeline, streaming, chunk scoring, Notion integration), what comes next

**Deliverable:** Test suite is meaningfully stronger. Eval baseline exists — we have a number to defend before the next prompt iteration. Portfolio doc exists for job conversations.

---

## What's Not In This Sprint

The following are explicitly deferred to future sprints:

| Item | Reason |
|---|---|
| Headless enrichment API / MCP server | Too early — needs real users and usage evidence first |
| Interactive tailoring format | Prerequisites: mature static surface + hosted fast model |
| Public profile chat | Prerequisites: high-quality extracted profiles + Haiku-class model |
| Evidence extraction architecture (two-phase chunk matching) | Needs eval baseline (Day 10) before committing to architecture change |
| Embeddings / pgvector | Needs evidence extraction architecture first |
| Platform partnership pitch (Simplify etc.) | Needs working product with user evidence |
| VNet integration for PostgreSQL | Destructive, ~$10-15/month overhead; revisit before compliance requirement |
| `next-test-api-route-handler` frontend API tests | Routes are thin proxies; testing them mostly tests the mock |
