# Remaining Work — Raw Dump 2026-07-18

*Generated by /archive-planning. Source files scanned: `33-sprint-plan-20260527.md`, `31-platform-integration-boundary.md`, `35-billing-data-model.md`, `41-github-silent-capture.md`, `42-sms-capture.md`, `43-repo-selector-ux.md` (design doc, no open items), `remaining-work-dump.md` (prior dump, items cross-referenced below).*

*`43-repo-selector-ux.md` contains no `[ ]` markers — design reference only.*

*Prior dump items that are now complete (dropped): `capture_signals` table, `is_duplicate_claim()` dedup service, `PendingReviewPanel` component, `bulk-review` endpoint, `pending` status on `ExperienceClaim`, `provenance_url`/`provenance_label` on `ExperienceClaim`.*

---

## From: 33-sprint-plan-20260527.md

### Day 21 — Experience Signal Layer + Dedup Schema Foundations

- [ ] Add `ExperienceSignal` ORM model to `database.py` (from "### New table: experience_signals")
- [ ] Alembic migration for `experience_signals` table (from "### New table: experience_signals")
- [ ] Add `supplementary` (JSONB nullable) to `experience_claims` (from "### Schema additions to experience_claims")
- [ ] Add `ownership` (varchar 20, default `"auto"`) to `experience_claims` (from "### Schema additions to experience_claims")
- [ ] Alembic migration for claim dedup columns (from "### Schema additions to experience_claims")
- [ ] Update `ExperienceClaim` ORM model with new fields and `signals` relationship (`lazy="select"`) (from "### ORM + type updates")
- [ ] Update `frontend/src/types/index.ts` — add `supplementary` and `ownership` to `ExperienceClaim` (from "### ORM + type updates")

### LLM Resilience

- [ ] Catch `openai.RateLimitError` in `llm_parse_with_retry` separately from validation errors (from "### 1 — llm_parse_with_retry: 429 backoff")
- [ ] Exponential backoff with jitter: start 1s, double, ±25% jitter, cap 30s (from "### 1 — llm_parse_with_retry: 429 backoff")
- [ ] Separate `rate_limit_retries` budget (default 4), distinct from `max_retries` (from "### 1")
- [ ] Log each backoff attempt as `llm_rate_limit_backoff` WARNING with attempt number and wait_ms (from "### 1")
- [ ] Increment `LLM_RETRIES_TOTAL` with `reason="rate_limit"` label (from "### 1")
- [ ] Add module-level asyncio `Semaphore` in `llm_utils.py` — configurable via `settings.llm_max_concurrent_requests` (default 16) (from "### 2 — Global LLM concurrency cap")
- [ ] Acquire semaphore in `llm_parse` and `llm_generate` before every LLM call (from "### 2")
- [ ] Expose current concurrency level as Prometheus gauge `llm_inflight_requests` (from "### 2")
- [ ] Write local load script `scripts/stress_test_tailoring.py` — N concurrent `POST /tailorings` requests (from "### 3 — Stress test")
- [ ] Run at N = 1, 2, 5, 10, 25 — document throughput ceiling and P95 latency (from "### 3")
- [~] AMP OTLP endpoint path uncertain — verify in staging by checking Container App logs for export errors (from "### Results")

---

## From: 31-platform-integration-boundary.md (Phase 1 — Schema + Boundary Doc)

- [ ] Alembic migration: add `pillar` (varchar 50 nullable) to `experience_claims` (from "### 1.1 Alembic migration")
- [ ] ORM model: add `pillar` field to `ExperienceClaim` in `database.py` (from "### 1.2 ORM model update")
- [ ] `experience_chunker.py` — set `status='pending'` for GitHub chunk creation (from "### 1.3")
- [ ] Filter `WHERE status = 'approved'` in `tailoring_generator.py` and `chunk_matcher.py` (from "### 1.4")
- [ ] Add `pillar` to `ExperienceClaimResponse` Pydantic schema and PATCH body model in `experience.py` (from "### 1.5")
- [ ] Add `pillar?: string` to `ExperienceClaim` TypeScript interface in `frontend/src/types/index.ts` (from "### 1.6")
- [ ] Add "Platform/Integration Boundary" section to `CLAUDE.md` (from "## Phase 2 — Boundary Documentation")
- [ ] Remove hardcoded `"GitHub:"` prefix in `chunk_matcher.py` — read from `provenance_label` instead (from "## Current State")

---

## From: 35-billing-data-model.md

- [ ] Implement `user_subscriptions` table + Alembic migration (from "## Open Items")
- [ ] Implement `user_entitlements` table + Alembic migration (from "## Open Items")
- [ ] Implement `referrals` table + Alembic migration (from "## Open Items")
- [ ] Write `entitlements.py` service with `get_effective_limits()` (from "## Open Items")
- [ ] Wire Stripe webhook handler → upsert `user_subscriptions` on subscription lifecycle events (from "## Open Items")
- [ ] Implement `/r/{code}` claim flow + activation trigger on tailoring `generation_status = "ready"` (from "## Open Items")
- [ ] Decide: referral cap (5/account) — lifetime or annual reset? (from "## Open Items")
- [ ] Decide: do pack-purchased tailorings for Capture users include Notion export? (from "## Open Items")
- [ ] Decide: does Capture tier include Notion export as part of base tier? (from "## Open Items")
- [ ] Decide: Premium tailoring limit — hard unlimited or soft cap with overage warning? (from "## Open Items")

---

## From: 41-github-silent-capture.md

### Phase 3 — GitHub App + Webhook Backend

- [ ] GitHub App registration (manual: create App, configure permissions + webhook URL, generate private key) (from "### Phase 3")
- [ ] `POST /integrations/github/webhook` — HMAC verification, event/bot/branch filtering, signal persist, BackgroundTask enqueue, 202 response (from "### Phase 3")
- [ ] User linking via `installation_id` on `ExperienceSource` (from "### Phase 3")
- [ ] Signal processor: fetch user's commits via GitHub API, assemble signal dict, LLM extraction call, dedup pass, insert `pending` claims (from "### Phase 3")
- [ ] GitHub App credentials wired into Key Vault and Container App env vars via Terraform (from "### Phase 3")
- [ ] Tests: webhook handler unit tests (HMAC, event filters, bot filter); extraction unit tests with fixture PR descriptions (from "### Phase 3")

### Phase 4 — GitHub App Installation UX

- [ ] "Connect GitHub App" button on Sources page → links to GitHub App installation page (from "### Phase 4")
- [ ] Post-install callback route — stores `installation_id` in `ExperienceSource.config`, upserts `UserIntegration(provider="github")` (from "### Phase 4")
- [ ] Initial light scan after callback — fetch installed repos, run light scan using installation token (from "### Phase 4")
- [ ] Sources page connected state: GitHub login, "Capture active" badge, Disconnect button (from "### Phase 4")
- [ ] Disconnect: removes `UserIntegration(provider="github")`, clears `installation_id` from `ExperienceSource.config` (from "### Phase 4")

### Phase 4c — Repo-to-Role Suggestion UX

- [ ] `PATCH /experience/groups/{id}` — accept `parent_id` field to link a repo group to a role group (from "### Phase 4c")
- [ ] `PATCH /experience/groups/{id}/dismiss-suggestion` — set `type_meta.suggestion_dismissed = true` (from "### Phase 4c")
- [ ] Frontend: read `type_meta.suggested_parent_id` + `suggestion_confidence`, render dismissable suggestion chip on repo group cards (from "### Phase 4c")
- [ ] Add `suggested_parent_name: str | None` to `type_meta` in `_store_suggestion` (from "### Phase 4c")

### Phase 4d — Claims Data Layer Cleanup

- [ ] Centralize `flattenClaims` and `removeClaimFromResponse` into `experience-claim-utils.tsx` (from "### Phase 4d")
- [ ] Lift `/claims` fetch into `ExperienceManager`, pass `data: ExperienceClaimsResponse` as prop to `ProfileChunkEditor` (eliminates duplicate network request) (from "### Phase 4d")

---

## From: 42-sms-capture.md

### Phase 1 — Phone Number Binding

- [ ] Backend: `POST /integrations/sms/verify/start` — Twilio Verify send OTP (from "### Phase 1")
- [ ] Backend: `POST /integrations/sms/verify/confirm` — check OTP, upsert `UserIntegration` (from "### Phase 1")
- [ ] Backend: `DELETE /integrations/sms` — disconnect binding (from "### Phase 1")
- [ ] Frontend API routes proxying the above (from "### Phase 1")
- [ ] Sources page: SMS card — Connect / Disconnect / phone number masked display (from "### Phase 1")
- [ ] Env vars and Key Vault entries for Twilio credentials (from "### Phase 1")
- [ ] Tests: verify start/confirm happy path and failure cases (from "### Phase 1")

### Phase 2 — Inbound Webhook + Signal Persistence

- [ ] `POST /integrations/sms/webhook` — Twilio signature verification, phone lookup, signal insert, 200 TwiML reply (from "### Phase 2")
- [ ] BackgroundTask stub (signal inserted, not yet processed) (from "### Phase 2")
- [ ] Tests: webhook handler (valid sig, invalid sig 403, unbound number, signal row created) (from "### Phase 2")

### Phase 3 — LLM Extraction + Reply

- [ ] SMS extraction prompt + `SMSClaimExtractionResult` Pydantic schema (from "### Phase 3")
- [ ] Background processor: extract claims, dedup pass, insert `pending` claims, send outbound reply via Twilio REST (from "### Phase 3")
- [ ] Skip/dedup reply variants (from "### Phase 3")
- [ ] Tests: extraction unit tests with fixture brain dumps (from "### Phase 3")

### Phase 4 — SMS Approval Commands

- [ ] Command parser: `APPROVE ALL`, `APPROVE 1 2`, `REJECT`, `REJECT 1` etc. (from "### Phase 4")
- [ ] Command handler: resolve pending claims from most recent signal, apply transitions, send confirmation reply (from "### Phase 4")
- [ ] Edge cases: no pending claims, invalid indices, already-approved claims (from "### Phase 4")
- [ ] Tests: command parsing unit tests, transition integration tests (from "### Phase 4")

---

## From: remaining-work-dump.md (carried forward — not yet completed)

Items dropped (completed since last archive): `capture_signals` table schema, `is_duplicate_claim()` dedup service, `PendingReviewPanel`, `bulk-review` endpoint, additive upload semantics, `SkillsModal`, SSRF protection in scraper, LLM cost observability dashboards, scrape path metrics.

### Frontend UX

- [ ] My Experience: always-visible section shells — render Resume, GitHub, Additional Experience, Inferred Profile shells with empty states (from "30-frontend-refactoring-plan.md")
- [ ] LinkedIn URL missing protocol — fix `authorLinkedin` in `CandidateFooter`; normalize at render + strip at extraction (from "24-sprint-plan-20260510-20260517.md")
- [ ] Mobile layout — tailoring detail header with View Posting / Copy / Regenerate / Export buttons needs dropdown at small screens (from "01-remaining-work-dump.md")
- [ ] Accessibility audit — missing `aria-label` on icon-only buttons, `role="alert"` on error states, keyboard nav through sidebar tailoring list (from "01-remaining-work-dump.md")
- [~] Reorder chunks within a group — `position` field in PATCH contract, no drag/arrow UI built (from "24-sprint-plan-20260510-20260517.md")
- [~] Merge chunks — endpoint done, no selection UI in `ChunkItem` (from "24-sprint-plan-20260510-20260517.md")
- [~] Design edit surface for the generated letter (from "24-sprint-plan-20260510-20260517.md")
- [~] `UserInputSection.tsx` not extracted from `ExperienceManager` (from "30-frontend-refactoring-plan.md")
- [~] bfcache/pageShowKey in `TailoringDetail.tsx` — attempted + reverted same session (from "18-chunk-hardening.md")

### LLM Pipeline

- [ ] Evidence extraction architecture (two-phase) — Phase 1: flat evidence claims from profile; Phase 2: scoring against evidence list; prevents inferred claims (from "01-remaining-work-dump.md")
- [ ] Section pre-filtering — classify sections as evaluable/non-evaluable before chunk enrichment; skip "Benefits", "About Us", "Compensation" etc. (from "01-remaining-work-dump.md")
- [ ] Candidate fact sheet indexing pass — single LLM call produces compact fact sheet; all chunk scoring uses fact sheet instead of full profile (from "01-remaining-work-dump.md")
- [ ] Targeted self-verification on Gap scores — second focused call for chunks scoring 0 on YOE, named technologies, education (from "01-remaining-work-dump.md")
- [ ] Prompt iteration — systematically review `generate_tailoring` system prompt; add few-shot examples to profile extraction prompt (from "01-remaining-work-dump.md")

### Chunk Hardening

- [ ] Gap response deduplication signal — when user regenerates after answering a gap, UI should handle gracefully if requirement no longer appears (from "18-chunk-hardening.md")
- [ ] Position ordering for user_input chunks — new chunks should set `position = max(position) + 1` (from "18-chunk-hardening.md")
- [ ] Embed-before-re-enrich ordering — gap response endpoint must embed synchronously before calling `re_enrich_single_chunk` (from "18-chunk-hardening.md")

### Infra & Observability

- [ ] GitHub App Terraform — wire `GITHUB_APP_ID`, `GITHUB_APP_INSTALLATION_ID`, `GITHUB_APP_PRIVATE_KEY` into backend Container Apps + Key Vault secrets (from "10-sprint-plan-20260421-20260425.md")
- [ ] GitHub App KV secret upload (manual step) — `az keyvault secret set` for prod + staging private keys, App IDs in `terraform.tfvars` (from "10-sprint-plan-20260421-20260425.md")
- [ ] Add `azurerm_monitor_scheduled_query_rules_alert_v2` in `monitoring.tf` for job scrape failure spikes (from "25-observability-strategy.md")
- [ ] Cloudflare API token — 1Password CLI injection: `op run -- terraform apply` (from "01-remaining-work-dump.md")
- [ ] Separate Tailord Staging Google OAuth client (from "01-remaining-work-dump.md")
- [ ] SARIF-based CVE monitoring when repo goes public — Trivy upload to GitHub Security (from "01-remaining-work-dump.md")
- [~] VNet integration for PostgreSQL — security debt; +$10–15/month; deferred until compliance requirement (from "01-remaining-work-dump.md")
- [~] AMP OTLP endpoint path — verify in staging (from "33-sprint-plan-20260527.md")
- [~] `make eval` in CI — manual; deferred until targeting hosted model (from "10-sprint-plan-20260421-20260425.md")

### Testing

- [ ] Backend test coverage to 80%+ — currently ~49%; gap: SSE streaming, background tasks, Notion export, experience endpoints (from "01-remaining-work-dump.md")
- [~] Frontend API route tests with next-test-api-route-handler — revisit if routes gain logic (from "01-remaining-work-dump.md")

### Developer Experience

- [ ] Observability standards Claude hook — post-edit hook warns when backend adds LLM call without OTel span, FastAPI endpoint without `require_api_key`, or `logging.getLogger` instead of `structlog` (from "24-sprint-plan-20260510-20260517.md")
- [ ] Claude Code: Subagent definitions — `.claude/agents/` with `backend.md`, `frontend.md`, `reviewer.md` (from "01-remaining-work-dump.md")
- [ ] Claude Code: GitHub MCP — configure for PR creation, issue viewing, CI status, PR comments (from "01-remaining-work-dump.md")
- [ ] Claude Code: PostgreSQL MCP — direct DB access for debugging migrations (from "01-remaining-work-dump.md")
- [ ] Portfolio write-up — `planning/12-portfolio-writeup.md`: problem, key technical decisions, what I'd do next (from "10-sprint-plan-20260421-20260425.md")

### Conversational Experience Agent

- [ ] Phase 1 — Quick Log UI on `/dashboard/experience`: textarea + confirm flow, `suggested_group_key` in `ParsedClaims` (from "28-conversational-experience-agent.md")
- [ ] Phase 2a — `ConversationThread` + `ConversationMessage` SQLAlchemy models + migration (from "28-conversational-experience-agent.md")
- [ ] Phase 2b — `POST /chat` endpoint: IntentClassification LLM call, `add_experience` handler (from "28-conversational-experience-agent.md")
- [ ] Phase 2c — Frontend chat panel: floating button → slide-out overlay on all dashboard pages (from "28-conversational-experience-agent.md")
- [ ] Phase 2d — `create_tailoring` intent handler: extract URL → kick off pipeline → follow-up message when ready (from "28-conversational-experience-agent.md")
- [ ] Phase 2e — `query` intent handler: profile Q&A using formatted profile as LLM context (from "28-conversational-experience-agent.md")
- [ ] Phase 3 — Attribution clarification loop: two-turn max, saves with `group_key = null` on skip (from "28-conversational-experience-agent.md")

### Platform Architecture

- [ ] Platform/Integration boundary audit — review FastAPI routes for dev-specific vocabulary in core platform (from "misc/geminiExchangePillars.md")
- [ ] Add `pillar` key standardization to `ExperienceClaim.chunk_metadata` for dev-integration-sourced claims (from "misc/geminiExchangePillars.md")
- [ ] Define and document 6 Product Pillars schema — finalize pillar names, structural signatures, competency keywords (from "misc/geminiExchangePillars.md")
- [ ] Wire pillar classification into GitHub enrichment LLM schema and prompt (from "misc/geminiExchangePillars.md")
- [ ] Digest notification system — weekly summary of pending claims; design schema + worker (from "misc/geminiExchangePillars.md")

### TAILORD.md Artifact

- [ ] Design TAILORD.md spec — repo-level manifest with `TAILORD.md` (public) + `.tailord/claims.json` (personal, gitignored) (from "misc/geminiExchangePillars.md")
- [ ] Build TAILORD.md for the Tailord repo itself as portfolio artifact (from "misc/geminiExchangePillars.md")
- [ ] Agentic TAILORD.md generation — background task: structural scan → draft manifest → user review (from "misc/geminiExchangePillars.md")

### Auth & Onboarding

- [ ] Email infrastructure — evaluate Resend/Postmark/SendGrid; design for magic link auth, weekly claim digest, account welcome (from "misc/geminiExchangePillars.md")
- [ ] Magic link login — email/magic-link as second auth method alongside Google OAuth (from "misc/geminiExchangePillars.md")
- [ ] LinkedIn OAuth + profile import — LinkedIn login; primary value is importing work history as bootstrap (from "misc/geminiExchangePillars.md")

### Firecrawl

- [ ] Implement Firecrawl free tier (500 scrapes/month) as replacement/supplement to Playwright scraper — target `backend/app/core/scraper.py` (from "misc/geminiExchangePillars.md")

### Product Features

- [ ] Interactive tailoring format — alternate view rendering job posting as frame with candidate experience woven in (from "01-remaining-work-dump.md")
- [ ] Tailorings on public profile — `show_on_profile` toggle per tailoring; candidate curates which appear (from "01-remaining-work-dump.md")
- [ ] Notion database export mode — export to Notion database with company, role, date, URL properties (from "01-remaining-work-dump.md")
- [ ] Interactive homepage demo — visitor pastes job URL → preview analysis without sign-up (from "01-remaining-work-dump.md")
- [ ] Homepage social proof section — "X tailorings generated", testimonials with outcomes (from "01-remaining-work-dump.md")
- [ ] Homepage ProductPreview — replace mockup with real screenshot of Fit Analysis view (from "01-remaining-work-dump.md")
- [~] Notion public integration review submission — deferred; submit once screenshots ready (from "01-remaining-work-dump.md")

### North Star

- [ ] Public profile chat interface — recruiter asks questions on `/u/{slug}`, answered from candidate experience (from "01-remaining-work-dump.md")
- [ ] Headless enrichment API for job boards — `POST /enrich` with partner API key; async job ID + poll/webhook (from "01-remaining-work-dump.md")
- [ ] MCP server for Tailord — expose experience, tailoring, analysis as MCP tools/resources (from "01-remaining-work-dump.md")
- [ ] AI job search assistant — cross-job intelligence: "which jobs best match my skills?", "what skills am I missing?" (from "01-remaining-work-dump.md")
- [ ] Platform partnership pitch to Simplify / Teal / Ashby (from "01-remaining-work-dump.md")
- [ ] OpenAPI schema cleanup — consistent response shapes, operation IDs, error codes (from "01-remaining-work-dump.md")
- [ ] JSON endpoint for public tailoring/profile pages — `?format=json` on `/u/{slug}` and `/t/{slug}` (from "01-remaining-work-dump.md")
- [ ] Webhooks for async enrichment events — `tailoring.ready`, `tailoring.enriched`, `experience.processed` (from "01-remaining-work-dump.md")
