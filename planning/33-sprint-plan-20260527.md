# Sprint Plan — 2026-05-27

**Started:** 2026-05-27
**Branch:** `dunbarchara/foundations`

---

## Day 0 — Experience Claim foundations

### Schema & naming

- [x] Finalized "Experience Claim" as the atomic unit name — user-facing and internal
- [x] Finalized "Experience Group" as the parent container — replaces the ad-hoc `group_key` string
- [x] Defined two claim states: grouped (`group_id` non-null) and ungrouped (`group_id` null)
- [x] Established the retrieval contract: groups travel with their claims to LLM — enforced at retrieval layer, not rendering

### Migration

- [x] Wrote Alembic migration `d2e3f4a5b6c7` — rename `experience_chunks` → `experience_claims`, rename index
- [x] Migration creates `experience_groups` table with all columns and FK to `experiences` (CASCADE)
- [x] Migration adds 6 new columns to `experience_claims`: `group_id` (FK, SET NULL), `confidence`, `status`, `provenance_url`, `provenance_label`, `tags`

### Backend models

- [x] Added `ExperienceGroup` ORM class to `database.py`
- [x] Renamed `ExperienceChunk` → `ExperienceClaim`, `__tablename__` = `"experience_claims"`
- [x] Added all new fields to `ExperienceClaim` with correct types and defaults
- [x] Updated `Experience` relationships: `chunks` → `claims`, added `groups`

### Backend services

- [x] `experience_chunker.py` — updated import and all class references
- [x] `experience_embedder.py` — updated import and class references (internal function names kept)
- [x] `chunk_matcher.py` — updated import and class references

### Backend API (`experience.py`)

- [x] Renamed routes: `/experience/chunks` → `/experience/claims`
- [x] Renamed routes: `/experience/chunks/{chunk_id}` → `/experience/claims/{claim_id}`
- [x] Renamed routes: `/experience/user-input/chunks` → `/experience/user-input/claims`
- [x] Renamed `ChunkContentUpdate` → `ClaimContentUpdate`
- [x] Renamed serializer `_serialize_exp_chunk` → `_serialize_exp_claim`
- [x] Renamed grouper `_group_experience_chunks` → `_group_experience_claims`
- [x] Updated return keys: `chunk_ids` → `claim_ids`, `chunk_id` → `claim_id`
- [x] Updated log events: `user_input_chunks_persisted` → `user_input_claims_persisted`

### Backend tests & scripts

- [x] `test_chunk_lifecycle.py` — updated model references and relationship accessor `["claims"]`
- [x] `export_mock_data.py` — updated model, builder function name, JSON output key

### Frontend types

- [x] `types/index.ts` — `ExperienceChunk` → `ExperienceClaim`, `ExperienceChunksResponse` → `ExperienceClaimsResponse`, section types updated

### Frontend API routes

- [x] Created `/api/experience/claims/route.ts` (GET)
- [x] Created `/api/experience/claims/[id]/route.ts` (PATCH, DELETE)
- [x] Created `/api/experience/user-input/claims/route.ts` (POST)
- [x] Overwrote old `/chunks` routes to proxy to new `/claims` backend endpoints (backward compat)

### Frontend components

- [x] `ProfileChunkEditor.tsx` — all type and fetch URL references updated to claims
- [x] `TailoringDetail.tsx` — type and fetch URL updated
- [x] `AnalysisView.tsx` — type updated
- [x] `ExperienceManager.tsx` — response type updated

### Frontend mock & demo

- [x] `mock/loader.ts` — `getMockExperienceChunks` → `getMockExperienceClaims`, data key updated
- [x] Demo experience page updated to use `getMockExperienceClaims`

### Documentation

- [x] `CLAUDE.md` — routing table, data models, and domain concepts updated
- [x] `planning/32-experience-claim-schema.md` — schema reference doc with field tables, type_meta shapes, source type lifecycle, confidence levels, chunk_metadata shapes, 5 mock JSON examples, LLM context rule, rejected options, deferred items

---

## Day 1 — Replace experience_id FK with user_id on Claims and Groups

### Migration

- [x] Wrote Alembic migration `e3f4a5b6c7d8` — backfills `user_id` on both tables from `experiences`, drops `experience_id` FK and index

### Backend models (`database.py`)

- [x] `ExperienceClaim.experience_id` → `user_id` (FK → users.id, CASCADE)
- [x] `ExperienceGroup.experience_id` → `user_id` (FK → users.id, CASCADE)
- [x] `ExperienceClaim.experience` relationship → `user` (back_populates="claims")
- [x] `ExperienceGroup.experience` relationship → `user` (back_populates="groups")
- [x] Removed `Experience.claims` and `Experience.groups` relationships
- [x] Added `User.claims` and `User.groups` relationships (cascade="all, delete-orphan")

### Backend services

- [x] `experience_chunker.py` — `_delete_chunks`, `delete_*` functions: `experience_id` → `user_id`; claim creation uses `user_id=experience.user_id`
- [x] `experience_embedder.py` — `embed_experience_chunks(user_id, db)`; `embed_experience_chunks_task(user_id)` (dropped redundant `user_id: str` log param)
- [x] `chunk_matcher.py` — all `experience_id` params renamed to `user_id` throughout (`enrich_job_chunks`, `re_enrich_single_chunk`, `refresh_job_chunks`, `_retrieve_top_k_experience_chunks`, `_score_chunk_vector`)
- [x] `github_enricher.py` — `embed_experience_chunks(experience.user_id, db)` (still receives `experience_id` to load Experience object)

### Backend API

- [x] `experience.py` — all `ExperienceClaim` queries and creations updated from `experience_id` to `user_id`
- [x] `tailorings.py` — `_finalize_tailoring` param `experience_id` → `user_id`; `_stream_tailoring` captures `user_id = user.id`; `refresh_job_chunks` call updated

### Tests & scripts

- [x] `test_chunk_lifecycle.py` — updated to use `user_id`; cascade test updated to check `User.claims`
- [x] `test_chunk_matcher.py` — `experience_id=` kwargs updated to `user_id=`
- [x] `export_mock_data.py` — updated to filter by `ExperienceClaim.user_id == experience.user_id`

### Documentation

- [x] `CLAUDE.md` — Data Models and Relationships sections updated

---

## Day 2 — User Identity & Data Model Refactor

Split the monolithic `users` table into four concern-separated tables. Removes null-bloat (5 Notion columns null for most users), decouples Google as the only auth provider, adds `updated_at`/`deleted_at` for audit and tombstone support.

### Migration

- [x] Wrote Alembic migration `a7b8c9d0e1f2` — single atomic migration (create + backfill + drop); runs at `alembic upgrade head` on startup, so two-migration split would not work with current deployment model
- [x] Creates `auth_identities` table (provider-neutral OAuth subjects; UNIQUE `(provider, subject)`)
- [x] Creates `user_profiles` table (1:1 with users; `username_slug` UNIQUE indexed; `communication_email` for future digest)
- [x] Creates `user_integrations` table (UNIQUE `(user_id, provider)`; JSONB `credentials` + `provider_metadata`)
- [x] Adds `updated_at`, `deleted_at` to `users`
- [x] Backfills `auth_identities` from `users.google_sub` (provider="google")
- [x] Backfills `user_profiles` from users profile fields (1 row per user)
- [x] Backfills `user_integrations` from users Notion fields (only where `notion_access_token IS NOT NULL`)
- [x] Drops all migrated columns from `users`: `google_sub`, profile fields (6), Notion fields (5) — 239 tests pass post-migration

### Backend models (`database.py`)

- [x] `User` stripped to: `id`, `email` (nullable), `name`, `status`, `is_admin`, `created_at`, `updated_at`, `deleted_at`
- [x] Added `AuthIdentity` model — `provider`, `subject`, `email`, `connected_at`, `updated_at`; back_populates User
- [x] Added `UserProfile` model — all profile fields + `communication_email`; back_populates User
- [x] Added `UserIntegration` model — `provider`, `credentials` (JSONB), `provider_metadata` (JSONB, DB col name `metadata`); back_populates User
- [x] `User.profile` relationship: `lazy="selectin"` (always co-loaded)
- [x] `User.integrations` relationship: `lazy="selectin"` (always co-loaded)
- [x] `User.candidate_name` property: delegates to `user.profile.preferred_first/last` → `user.name` → `user.email`

### Backend auth (`deps_user.py`)

- [x] `get_current_user`: looks up `AuthIdentity(provider="google", subject=x_user_id)` instead of `User.google_sub`
- [x] New user path: creates `User` → `UserProfile` (with avatar_url) → `AuthIdentity` atomically; double-IntegrityError catch for race conditions
- [x] `_generate_username_slug`: queries `UserProfile.username_slug` (was `User.username_slug`)

### Backend API (`users.py`)

- [x] `_user_response()`: flattens `user.profile.*` into response; adds `communication_email`; removes `google_sub` from response; reads `notion_workspace_name` from `user.integrations`
- [x] `update_user()`: writes to `user.profile` instead of `user` directly; adds `communication_email` to `UserUpdate` schema
- [x] `check_username()`: queries `UserProfile.username_slug`
- [x] `get_public_user()`: queries `UserProfile` by `username_slug`, then loads `profile.user`
- [x] `delete_user()`: tombstone pattern — hard-deletes all cascade data, sets `deleted_at`, clears `email`/`name` PII, keeps `users` row

### Backend API (`notion.py`)

- [x] `notion_callback()`: upserts `UserIntegration(provider="notion")` with `credentials={access_token}`, `provider_metadata={bot_id, workspace_id, workspace_name}`
- [x] `notion_disconnect()`: deletes `UserIntegration` row instead of nulling user fields
- [x] `export_tailoring_to_notion()`: reads `integration.credentials["access_token"]`, `integration.provider_metadata["parent_page_id"]`; locks integration row with `with_for_update()`; on `NotionAuthError`: `db.delete(integration)` instead of nulling 5 fields

### Backend services

- [x] `tailorings.py` — 8 sites: `user.pronouns` → `user.profile.pronouns if user.profile else None`; `user.preferred_*` → `user.candidate_name`; `author.username_slug` → `author.profile.username_slug`; `author.profile_public` → `author.profile.profile_public`
- [x] `tailorings.py` — public tailoring lookup: `User.username_slug ==` → `UserProfile.username_slug ==` (queries profile, then loads user)
- [x] `experience.py` — 2 sites: `user.pronouns` → `user.profile.pronouns`; storage key: `users/{user.google_sub}/` → `users/{user.id}/`
- [x] `admin.py` — `user.avatar_url` → `user.profile.avatar_url if user.profile else None`
- [x] `services/gap_analyzer.py` — `user.pronouns` → `user.profile.pronouns if user.profile else None`

### Tests

- [x] `conftest.py` — `make_user()`: creates `User` + `UserProfile` + `AuthIdentity`; `google_sub=` param retained as the `AuthIdentity.subject`
- [x] `test_notion.py` — `_user_with_notion()`: creates `UserIntegration` row directly instead of passing notion kwargs to `make_user`
- [x] `test_tailorings.py` — `approved_user.username_slug` → `approved_user.profile.username_slug` (3 sites)
- [x] `test_users.py` — `approved_user.username_slug` → `approved_user.profile.username_slug` (3 sites)
- [x] `tests/services/test_gap_analyzer.py` — `tailoring.user.pronouns` → `tailoring.user.profile.pronouns`

### Documentation

- [x] `CLAUDE.md` — Data Models section replaced with 4 new/updated model entries; identity lookup pattern noted
- [x] `planning/32-experience-claim-schema.md` — added note on `a7b8c9d0e1f2` migration affecting parent `users` table
- [x] `planning/34-database-schema-review.md` — `users` section updated; 3 new table sections added; cross-cutting and decisions tables updated

---

## Day 3 — Experience Sources Refactor + Letter Generator Rename

Replace the monolithic `experiences` table with `experience_sources` (one row per `(user_id, source_type)`). Rename `tailoring_generator.py` → `letter_generator.py`.

### Migration

- [x] Wrote Alembic migration `b4d5e6f7a8b9` — CREATE `experience_sources`, backfill resume + github rows from `experiences` (with `::jsonb` casts since `extracted_profile` is `json` type, not `jsonb`; `uploaded_at` used in place of nonexistent `created_at`), DROP `experiences`; downgrade recreates `experiences` and backfills back with `::json` casts — both directions verified with `upgrade head` / `downgrade -1` / `upgrade head` cycle

### Backend models (`database.py`)

- [x] Removed `Experience` ORM class entirely
- [x] Added `ExperienceSource` — `source_type` (varchar 30), `connection_status`, `sync_status`, `last_synced_at`, `last_requested_at`, `error_message`, `config` (JSONB), `source_data` (JSONB), `created_at`, `updated_at`; UNIQUE `(user_id, source_type)`
- [x] `User.experience` (1:1) → `User.experience_sources` (1:many, `lazy="selectin"`)

### Profile assembly (`profile_formatter.py`)

- [x] Added `sources_to_profile_dict(sources: list[ExperienceSource]) -> dict` — assembles `{resume, corrections, github}` dict from sources list; used in place of `experience.extracted_profile` everywhere

### Backend services

- [x] `experience_processor.py` — queries `ExperienceSource(source_type="resume")` by `user_id`; writes to `source_data["extracted"]` / `source_data["raw_text"]`; sets `sync_status` during processing
- [x] `github_enricher.py` — gets/creates `ExperienceSource(source_type="github")`; writes to `source_data["repos"]` / `source_data["repo_details"]`; `source_id` param replaces old `experience_id`
- [x] `experience_chunker.py` — accepts `ExperienceSource` objects; reads `source_data["extracted"]` for resume, `source_data["repos"]` + `source_data["repo_details"]` for github
- [x] `gap_analyzer.py` — replaced `user.experience.extracted_profile` with `sources_to_profile_dict(user.experience_sources)`

### Letter generator rename

- [x] Created `letter_generator.py` — canonical copy of `tailoring_generator.py` with `generate_tailoring` renamed to `generate_letter`
- [x] `tailoring_generator.py` — replaced with compat stub: `from app.services.letter_generator import generate_letter as generate_tailoring; __all__ = ["generate_tailoring"]`
- [x] `tailorings.py` — imports `generate_letter` from `letter_generator` directly; `CLAUDE.md` key file paths updated

### Backend API (`experience.py`)

- [x] Full rewrite — all endpoints updated to `ExperienceSource`; removed `_ensure_experience`, `_clear_resume_fields`, `_has_non_resume_sources` helpers (no longer needed)
- [x] Added `_source_status(src)` helper — derives legacy status string from `connection_status` + `sync_status`
- [x] Added `_experience_response(sources)` — returns both new `sources[]` array (per-source status) AND all legacy flat fields (`status`, `filename`, `github_username`, `github_repos`, etc.) for frontend backward compat
- [x] `gap_answer` endpoint — creates `ExperienceClaim(source_type="gap_response")` instead of mutating `experience.user_input_text`

### Backend API (`tailorings.py`, `users.py`)

- [x] `tailorings.py` — all `experience.extracted_profile` replaced with `sources_to_profile_dict(user.experience_sources)`; `_validate_profile` fixed to check `(profile.get("github") or {}).get("repos")` (not old `"github_repos"` key); `gap_answer`, `rescore`, `debug`, `refresh`, public tailoring endpoints all updated
- [x] `users.py` — `Experience` → `ExperienceSource`; `delete_user` iterates `user.experience_sources` for storage cleanup; `get_public_user` iterates sources for resume/github data

### Tests

- [x] `conftest.py` — `make_experience_source(db, user, source_type, **kwargs)` factory added; `make_experience()` kept as backward-compat alias
- [x] `test_experience_chunker.py` — `_make_experience()` split into `_make_resume_source()` + `_make_github_source()` stubs matching new shape; all 20 tests pass
- [x] `test_gap_analyzer.py` — mock updated: `experience_sources=[MagicMock()]` (non-empty); `sources_to_profile_dict` patched in 3 `run_gap_analysis` tests; all pass
- [x] `test_tailoring_utils.py` — `_validate_profile` tests updated: `{"github_repos": [...]}` → `{"github": {"repos": [...]}}`
- [x] 239 tests pass total

### Frontend

- [x] `types/index.ts` — added `ExperienceSourceStatus` interface; `ExperienceRecord.id` typed as `string | null`; `sources?: ExperienceSourceStatus[]` added
- [x] `ExperienceManager.tsx` — `record.id ?? ''` coercion for `experienceId` field (TS strict null fix)

### Documentation

- [x] `CLAUDE.md` — `Experience` replaced with `ExperienceSource` in Data Models; relationships section updated (`User.experience_sources` 1:many selectin); profile assembly pattern noted; `letter_generator.py` replaces `tailoring_generator.py` in key file paths table

---

## Day 4 — Credential Encryption at Rest

Implements transparent Fernet encryption for `user_integrations.credentials` via a SQLAlchemy `TypeDecorator`. Zero impact on callers — `credentials` attribute still typed `dict | None`.

### Backend

- [x] Created `backend/app/core/crypto.py` — `EncryptedJSON` TypeDecorator; Fernet encrypt on write, decrypt on read; plaintext fallback for legacy rows (migration period) and unset key (local dev); logs error + returns None on unparseable value
- [x] `backend/app/config.py` — added `field_encryption_key: str | None = None`; generate with `uv run python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"`; no new dependency (`cryptography==47.0.0` already in pyproject.toml)
- [x] Alembic migration `c5d6e7f8a9b0` — ALTER COLUMN `user_integrations.credentials` JSONB → Text; PostgreSQL `::text` cast preserves existing plaintext JSON; downgrade casts back via `::jsonb` (will fail if encrypted rows exist — expected)
- [x] `backend/app/models/database.py` — `credentials` column type changed from `JSONB` to `EncryptedJSON`; Security TODO docstring removed

### Infrastructure

- [x] `infra/providers/azure/keyvault.tf` — added two `data` sources: `prod-field-encryption-key`, `staging-field-encryption-key` (pre-populated manually, never passed as Terraform variables)
- [x] `infra/providers/azure/main.tf` — both backend Container Apps (`backend_prod`, `backend_staging`) wired with `secret {}` block (KV reference) and `env { FIELD_ENCRYPTION_KEY }` injection
- [x] `infra/providers/azure/BOOTSTRAP.md` — step 3a updated with `az keyvault secret set` commands for both keys and generation instructions

### Documentation

- [x] `CLAUDE.md` — `UserIntegration.credentials` entry updated: Security TODO → implemented; column type noted as Text with EncryptedJSON/Fernet reference
- [x] `planning/34-database-schema-review.md` — `user_integrations` table and decisions table updated

---

## Day 5 — Schema Cleanup & Enrichment (Phases 1–4)

Addressed accumulated technical debt and added schema foundations for deduplication, silent capture, experience editor reordering, and multi-industry support. Scoped from `planning/34-database-schema-review.md`. Phase 5 (`group_key` drop) deferred — still the primary grouping mechanism in `chunk_matcher.py`.

### Phase 1 — Drop `experience_source` (singular) from `job_chunks`

- [x] Alembic migration `d1a2b3c4e5f6` — `DROP COLUMN experience_source`; downgrade backfills from `experience_sources->>0`
- [x] `backend/app/models/database.py` — removed `experience_source: Mapped[str | None]` from `JobChunk`
- [x] `backend/app/api/tailorings.py` — `_serialize_chunk()`: removed `"experience_source"` key; `source_label` now derived from `experience_sources[0]`. Same fix in rescore endpoint return dict.
- [x] `backend/app/services/notion_export.py` — `chunks_to_notion_markdown()`: source label derived from `chunk.experience_sources or []` first element
- [x] `backend/app/api/experience.py` — gap_response return dict: `"experience_source"` → `"experience_sources"`
- [x] `backend/tests/unit/test_notion_export.py` — `_chunk()` fixture: `experience_source=None` → `experience_sources=None`; `test_advocacy_blurb_in_callout` and `test_source_labels` updated to pass `experience_sources=[...]`
- [x] `backend/tests/services/test_chunk_matcher.py` — removed invalid `experience_source="resume"` kwarg from `ChunkMatchResult` fixture (not in schema)

### Phase 2 — `experience_claims` enrichment

- [x] Alembic migration `e2b3c4d5f6a7` — single revision: rename `technologies→keywords`; add `merged_from JSONB`, `original_content text`, `provenance_metadata JSONB`; drop `chunk_metadata`, `provenance_url`, `provenance_label` (backfills `provenance_metadata` from `chunk_metadata` before drop)
- [x] `backend/app/models/database.py` — `ExperienceClaim`: `technologies` → `keywords`; removed `chunk_metadata`, `provenance_url`, `provenance_label`; added `provenance_metadata`, `original_content`, `merged_from`; updated `status` docstring to `pending | active | archived`
- [x] `backend/app/api/experience.py` — `_serialize_exp_claim`: returns `keywords`, `provenance_metadata`, `original_content`, `status` (was missing all four); `ClaimContentUpdate`: added `status` field + updated validator; PATCH handler: `original_content` set on first edit; `status` patch validates `active | archived` only; gap creation uses `provenance_metadata=` and `keywords=` throughout (3 sites)
- [x] `backend/app/api/tailorings.py` — gap_answer creation: `technologies=None` → `keywords=None`; `chunk_metadata=` → `provenance_metadata=`
- [x] `backend/app/services/experience_chunker.py` — all chunk dicts: `"technologies"` → `"keywords"` (replace_all; 6 occurrences)
- [x] `backend/app/services/chunk_matcher.py` — `_retrieve_top_k_experience_chunks`: added `ExperienceClaim.status == "active"` allowlist filter (catches both `pending` and `archived`; safer than exclusion list for future status values)
- [x] `frontend/src/types/index.ts` — `ExperienceClaim`: `technologies` → `keywords`, `chunk_metadata` → `provenance_metadata`, added `original_content`, `status`
- [x] `frontend/src/components/dashboard/AnalysisView.tsx` — `chunk_metadata` → `provenance_metadata` (replace_all; 6 occurrences); `experience_source` fallback removed
- [x] `frontend/src/components/dashboard/ProfileChunkEditor.tsx` — local claim object: `technologies: null` → `keywords: null`, `chunk_metadata: null` → `provenance_metadata: null`, added `original_content: null`, `status: 'active' as const`; `c.chunk_metadata?.question` → `c.provenance_metadata?.question` (2 sites)
- [x] `backend/tests/services/test_experience_chunker.py` — `test_resume_chunks_projects_with_technologies`: `proj[0]["technologies"]` → `proj[0]["keywords"]`

### Phase 3 — `experience_groups` enrichment

- [x] Alembic migration `f3c4d5e6a7b8` — adds `position integer` (backfilled from `created_at` order), `provenance_url varchar(500)`, `provenance_label varchar(255)`, `tags JSONB`, `description text`; casts `type_meta JSON → JSONB`
- [x] `backend/app/models/database.py` — `ExperienceGroup`: added all 5 new fields; `type_meta` type changed from `JSON` to `JSONB`

### Phase 4 — Infrastructure cleanups

- [x] Alembic migration `a4b5c6d7e8f9` — `jobs.user_id` non-nullable; drops + recreates `tailorings_user_id_fkey` with `ON DELETE CASCADE`
- [x] `backend/app/models/database.py` — `Job.user_id`: `Mapped[uuid.UUID | None]` → `Mapped[uuid.UUID]`, `nullable=False`; `Job.user` relationship: `User | None` → `User`; `Tailoring.user_id`: FK updated to `ondelete="CASCADE"`
- [x] `backend/app/api/tailorings.py` — added `_cleanup_old_trigger_logs(db)` helper (deletes rows >30 days old); wired as `background_tasks.add_task` in both POST /tailorings and POST /tailorings/{id}/regenerate

### Frontend cleanup (cross-phase)

- [x] `frontend/src/types/index.ts` — `JobChunk`: removed `experience_source` field
- [x] `frontend/src/components/dashboard/JobPosting.tsx` — removed `experience_source` from rescore update spread and temp chunk object; source display simplified to `experience_sources` only
- [x] `frontend/src/components/dashboard/RequirementAnalysis.tsx` — `experience_source` fallback removed from both `chunkToMarkdown` and `ChunkRow` (replace_all)
- [x] `frontend/src/__tests__/chunks.test.ts` — removed `experience_source: null` from `makeChunk` fixture

### Results

- [x] 239 backend tests pass (ruff + bandit + pip-audit + pytest) — 0 new failures
- [x] Frontend build passes (eslint + tsc + jest 26 tests) — no type errors
- [x] `planning/34-database-schema-review.md` — all 4 phases reflected: column tables updated, cascade table updated, decisions table updated (10 items marked ✅ done, 3 remaining open)

---

## Day 6 — Gap Response Retrieval Quality

Two targeted fixes to ensure gap answers actually surface in LLM scoring context.

### Problem 1 — Gap prefix inflates embedding distance

`content` for `gap_response` claims includes display prefix `[Gap answer — {label}]: ...`. This is noise in embedding space — the cosine distance reflects the label, not the answer. Gap answers were competing poorly against resume bullets on exact-match queries.

- [x] `backend/app/services/experience_embedder.py` — added `_CLAIM_PREFIX_RE` regex and `_embedding_text(claim)` helper; strips `[Gap answer — ...]: ` prefix before calling `embed_text` for `gap_response` and `partial_response` source types; applied at all 3 embedding sites (`embed_experience_chunks` loop, `re_embed_chunk`)

### Problem 2 — Gap answers not guaranteed in top-K

Even with correct embeddings, if K resume bullets score higher cosine similarity, the gap answer is excluded from the LLM scoring context. A user who directly answers a requirement shouldn't lose to passive resume bulk.

- [x] `backend/app/services/chunk_matcher.py` — added `_append_pinned_claims(top_k, job_chunk_id, user_id, db)` helper; claims linked to a specific `job_chunk_id` via `provenance_metadata["job_chunk_id"]` are always appended to the top-K set regardless of cosine rank
- [x] `_score_chunk_vector`: added `pinned_job_chunk_id: str | None = None` param; calls `_append_pinned_claims` after retrieval when set
- [x] `re_enrich_single_chunk`: passes `pinned_job_chunk_id=chunk_id` to `_score_chunk_vector` so rescoring always includes directly-linked gap claims

### TODO logged

- [x] Added TODO comment in `tailorings.py` near gap answer creation: normalize conversational gap responses into "claim-speak" before ingestion (separate task — tracked in `planning/34-database-schema-review.md`)

### Test fixes

- [x] `backend/tests/services/test_experience_embedder.py` — `_make_chunk()`: added `source_type="resume"` field; `_embedding_text` access on chunks without `source_type` was `AttributeError` (3 tests fixed: `test_embeds_chunks_with_null_embedding`, `test_continues_after_per_chunk_failure`, `test_re_embed_chunk_updates_embedding`)
- [x] `backend/tests/services/test_chunk_matcher.py` — `test_re_enrich_vector_path_updates_chunk_fields`: added `mock_db.query.return_value.filter.return_value.all.return_value = []` so `_append_pinned_claims` returns top-k unchanged in the test context

### Results

- [x] 239 backend tests pass — 0 failures (was 4 failing before fixes)


























---

## Day 7 — Jobs & Job Chunks Refactor

Full implementation of `planning/36-jobs-refactor.md`. All 7 design decisions shipped in one pass.

### Dead code removal

- [x] Deleted `requirement_matcher.py` and `requirement_matching.py` (dead — never called from active app code)
- [x] Deleted `tests/services/test_requirement_matcher.py`
- [x] Removed `RequirementMatch` and `RequirementMatchList` from `schemas/matching.py`
- [x] Removed stale `requirement_matching` import from debug-info endpoint in `tailorings.py`

### Alembic migration (`c9d0e1f2a3b4`)

- [x] `is_requirement` → `include_in_scoring` column rename (Boolean, NOT NULL, default true)
- [x] Added `semantic_type VARCHAR(30) nullable` to `job_chunks`
- [x] Added `created_at`, `updated_at TIMESTAMPTZ NOT NULL DEFAULT now()` to `job_chunks`
- [x] Added `evaluation_status VARCHAR(20) nullable` to `job_chunks`; data migration backfills from `match_score`: 0/1/2 → `scored`, -1 + "error" in rationale → `error`, -1 otherwise → `skipped`
- [x] Added `source_type VARCHAR(20) NOT NULL DEFAULT 'url'` to `jobs`

### ORM model (`database.py`)

- [x] `JobChunk`: `is_requirement` → `include_in_scoring`; added `semantic_type`, `evaluation_status`, `created_at`, `updated_at`
- [x] `Job`: added `source_type` with `default="url"`, `server_default="url"`

### Pydantic schema (`schemas/matching.py`)

- [x] `ChunkMatchResult`: added `include_in_scoring: bool = True` and `semantic_type: str = "other"`

### Semantic type rules + helpers (`chunk_matcher.py`)

- [x] Added `SEMANTIC_TYPE_RULES` dict — deterministic `(include_in_scoring, should_render)` overrides per type; `None` means LLM-decided
- [x] Added `resolve_chunk_flags(result)` — applies rules; `other` and `company_description.include_in_scoring` pass through LLM values unchanged
- [x] Added `_derive_evaluation_status(match, chunk_type)` — maps score + flags to `scored | skipped | error | None`

### Prompt update (`prompts/chunk_matching.py`)

- [x] Added `SEMANTIC_TYPE` section with 8-value taxonomy and `company_description` nuance note
- [x] Updated JSON output schema in both `USER_TEMPLATE` and `USER_TEMPLATE_VECTOR` to include `include_in_scoring` and `semantic_type`
- [x] Updated Example 3 to show all new fields (compensation → `include_in_scoring: false`, EEO → `application_info`)

### Pipeline service (`chunk_matcher.py`)

- [x] `enrich_job_chunks`: calls `resolve_chunk_flags` on each match before persisting; sets `include_in_scoring`, `semantic_type`, `evaluation_status` on new `JobChunk` rows
- [x] `refresh_job_chunks`: filter changed to `c.include_in_scoring` (was `c.is_requirement`); sets `evaluation_status` on in-place updates; does NOT touch `semantic_type` or `include_in_scoring`
- [x] `re_enrich_single_chunk`: sets `evaluation_status` after scoring
- [x] `_score_chunk_vector`: annotated result carries `include_in_scoring` and `semantic_type` through

### API (`tailorings.py`)

- [x] `_serialize_chunk`: removed `is_requirement`; added `include_in_scoring`, `semantic_type`, `evaluation_status`
- [x] `PatchChunkRequest`: `is_requirement` → `include_in_scoring`
- [x] PATCH handler: `chunk.is_requirement` → `chunk.include_in_scoring`
- [x] Manual chunk creation: `is_requirement=True` → `include_in_scoring=True, evaluation_status=None`
- [x] Rescore endpoint response: updated to `include_in_scoring`, `semantic_type`, `evaluation_status`
- [x] `Job(...)` construction: added `source_type="manual" if is_manual else "url"`

### Frontend

- [x] `types/index.ts` — `JobChunk`: removed `is_requirement`; added `include_in_scoring: boolean`, `semantic_type: string | null`, `evaluation_status: string | null`
- [x] `JobPosting.tsx` — all 6 `is_requirement` → `include_in_scoring` (replace_all); added `semantic_type: null, evaluation_status: null` to temp chunk literal
- [x] `TailoringDetail.tsx` — 2 `is_requirement` → `include_in_scoring` (dirty-check and patch build)

### Tests

- [x] `conftest.py` — `make_job`: added `source_type="url"` param
- [x] `tests/services/test_chunk_matcher.py` — imported `resolve_chunk_flags`, `_derive_evaluation_status`; added 12 new tests: `resolve_chunk_flags` (4 cases), `_derive_evaluation_status` (4 cases), `evaluation_status` set on enrich, header gets `skipped`, `semantic_type` not modified by refresh
- [x] `frontend/src/__tests__/chunks.test.ts` — `is_requirement` → `include_in_scoring`; added `semantic_type: null, evaluation_status: null` to `makeChunk` fixture

### Debug view updates

- [x] `RequirementAnalysis.tsx` — `ChunkRow`: split metadata into 2 rows; row 2 always shows `semantic_type`, `eval_status`, `include_in_scoring`, `render` (all fields, `—` for null); advocacy row always rendered; added `BoolField` for false-highlighted booleans; `chunkToMarkdown` always includes all new fields
- [x] `DebugPanel.tsx` — removed `requirement_matching_system_prompt` from `DebugInfo` type, rendered section, and Full Debug Dump copy

### Results

- [x] 241 backend tests pass (up from 229 — 12 new tests); 0 failures
- [x] Frontend: `tsc --noEmit` clean; Jest 26 tests pass
- [x] Evaluated chunk analysis against mock Linear job posting — `semantic_type` and `evaluation_status` rendering confirmed correct; one potential prompt fix (mixed-intent paragraph classification) deferred pending more real-world data

### Scripts

- [x] `scripts/export_mock_data.py` — stale `jc.experience_source` reference updated to `experience_sources[0]`

---

TODO: pass answer through a claim normalisation LLM step before storing.
Users often phrase gap answers as conversational replies ("Yes, I did X at Y") rather than claim-speak ("Did X at Y"). A normalisation pass would rewrite all claims into first-person declarative form ("Led X at Y") — improving embedding quality and LLM context. This applies to gap_response, partial_response, and user_input source types. ALSO: deduping could be used to link gap responses to existing entities like work experience. Replying 'used React at Microsoft' probably should be included under the role grouping and deduped in that boundary.

TODO: Dedup and normalize — similar claims in different groups are distinct, and should remain separate. But deduping could and should happen within a group boundary. Ungrouped claims could also be normalized into a group with user approval.

---

## Day 8 — Tailorings Schema Cleanup + Letter Regen Feature

Implemented `planning/34-database-schema-review.md` sprint: collapsed flat telemetry/Notion/model columns into JSONB, dropped redundant status fields, added `updated_at`, fixed slug collision, added structured letter rendering and "Upgrade Letter" upgrade path. See plan file at `.claude/plans/elegant-bouncing-crab.md`.

### Migration (`d0e1f2a3b4c5`)

- [x] Added `updated_at TIMESTAMPTZ` (`server_default=now()`, `onupdate=now()`) — was missing from tailorings
- [x] Added `generation_telemetry JSONB` — migrated from `generation_duration_ms`, `matching_mode`, `chunk_batch_count`, `chunk_error_count` via `jsonb_strip_nulls(jsonb_build_object(...))`
- [x] Added `notion_export JSONB` — migrated from 5 flat `notion_*` columns via `jsonb_strip_nulls`
- [x] Added `models JSONB` — migrated from flat `model` varchar via `jsonb_build_object('letter', model)`
- [x] Promoted `letter_content` JSON → JSONB via `postgresql_using='letter_content::jsonb'`
- [x] Dropped: `enrichment_status`, `gap_analysis_status`, `generation_duration_ms`, `chunk_batch_count`, `chunk_error_count`, `matching_mode`, `model`, `notion_container_page_id`, `notion_page_id`, `notion_page_url`, `notion_posting_page_id`, `notion_posting_page_url` (13 columns total)

### Backend ORM (`database.py`)

- [x] Removed 13 dropped columns from `Tailoring` class
- [x] Added `generation_telemetry`, `notion_export`, `models` JSONB fields
- [x] Added `updated_at` with `server_default=func.now()`, `onupdate=func.now()`
- [x] `letter_content` column type changed `JSON` → `JSONB`
- [x] Added deprecation comment on `generated_output`

### Backend API (`tailorings.py`)

- [x] `_finalize_tailoring`: `tailoring.model` → `tailoring.models = {"letter": settings.llm_model}`; telemetry now merges into `generation_telemetry` JSONB dict; removed `enrichment_status` write
- [x] `_stream_tailoring` regen reset: removed `enrichment_status = "pending"` and `gap_analysis_status = "pending"` writes
- [x] Extracted `_compute_base_slug`; `_generate_slug` now accepts `db` + `user_id`, checks uniqueness per user with up to 5 retries using `secrets.token_urlsafe`
- [x] `retry_gap_analysis`: `gap_analysis_status = "pending"` → `gap_analysis = None`; removed `gap_analysis_status` from response
- [x] `get_tailoring`: stale gap_analysis chunk refs filtered; response uses `models`, `generation_telemetry`, `notion_export` unpacked flat for API compat; added `updated_at`; removed `gap_analysis_status`
- [x] `get_tailoring_chunks`: `enrichment_status` derived at read time — `"complete" if chunks else "pending"`
- [x] `get_tailoring_debug_info`: reads from `generation_telemetry` JSONB and `models` JSONB
- [x] New `_run_letter_regen` background task: re-scores existing chunks + regenerates letter + runs gap analysis (no scraping)
- [x] New `POST /tailorings/{id}/regenerate-letter` SSE endpoint — for upgrading pre-`letter_content` rows

### Backend services (`chunk_matcher.py`)

- [x] Removed `_set_enrichment_status` helper and all call sites
- [x] Batch telemetry update: raw SQL `jsonb_build_object` merge into `generation_telemetry` JSONB (replaces `enrichment_status` + flat column writes)
- [x] Import cleanup: removed `Tailoring` from import (no longer used)

### Backend services (`gap_analyzer.py`)

- [x] Removed all `gap_analysis_status` writes
- [x] All early-exit paths now set `tailoring.gap_analysis = []` — `!= null` is now the reliable polling signal

### Backend API (`notion.py`)

- [x] Letter export prefers `letter_content`: re-renders via `_render_tailoring(TailoringContent(...))` when available, falls back to `generated_output`
- [x] All `notion_*` column reads/writes replaced with `notion_export` JSONB dict operations

### Frontend types (`types/index.ts`)

- [x] Removed `generation_duration_ms`, `chunk_batch_count`, `chunk_error_count`, `gap_analysis_status`
- [x] Added `models?: { letter?: string; scoring?: string } | null`, `generation_telemetry?: {...} | null`, `updated_at?: string | null`

### Frontend (`TailoringDetail.tsx`)

- [x] Gap analysis polling: replaced `gap_analysis_status === 'complete'` checks with `gap_analysis != null` sentinel (3 sites)
- [x] `handleCopy`: uses `letterContentToText(letter_content)` when available; falls back to `generated_output`
- [x] `canCopy`: `!!generated_output` → `!!letter_content || !!generated_output`
- [x] Added `letterContentToText()` helper (renders `LetterContent` → plain text for clipboard)
- [x] Added `handleLetterRegen` + state (`letterRegening`, `letterRegenSsePhase`) — SSE to new endpoint, same pattern as `handleRegenerate`
- [x] Added "Upgrade Letter" notice in letter tab — shown when `generation_status === 'ready' && !letter_content && !!generated_output`
- [x] Removed obsolete `gap_analysis_status === 'error'` retry button block

### Frontend API route (`regenerate-letter/route.ts`)

- [x] Created `frontend/src/app/api/tailorings/[id]/regenerate-letter/route.ts` — POST proxy via `proxyStreamToBackendWithUser`

### Schema doc (`planning/34-database-schema-review.md`)

- [x] `tailorings` table section fully rewritten to reflect post-migration schema — 13 dropped columns documented, new JSONB fields documented, sentinel behaviour noted
- [x] Decisions table: 5 new ✅ Done entries added
- [x] Post-next-deploy cleanup table: tailorings columns added (dropped + `generated_output` deferred)

---

## Day 9 — `llm_trigger_log` → `llm_usage_logs` Refactor

Renamed and extended `llm_trigger_log` to support billing quota enforcement and June cost tracking. See `planning/35-billing-data-model.md` context.

### Migration (`e0f1a2b3c4d5`)

- [x] `op.rename_table('llm_trigger_log', 'llm_usage_logs')`
- [x] Added `model VARCHAR(100)`, `input_tokens INTEGER`, `output_tokens INTEGER`, `cost_usd NUMERIC(10,6)` — all nullable until LLM instrumentation ships
- [x] Data migration: `event_type = 'experience_process'` → `'resume_process'`
- [x] Created composite index `ix_llm_usage_logs_user_event_time` on `(user_id, event_type, created_at)` — covers hourly burst queries and calendar-month quota range scans
- [x] Dropped auto-generated FK `llm_trigger_log_user_id_fkey`; recreated as `llm_usage_logs_user_id_fkey`

### Backend ORM (`database.py`)

- [x] Renamed `LlmTriggerLog` → `LlmUsageLog`, `__tablename__` = `"llm_usage_logs"`
- [x] Updated docstring with full event_type taxonomy and purpose summary
- [x] Added `model`, `input_tokens`, `output_tokens`, `cost_usd` mapped columns
- [x] Added `Numeric` to SQLAlchemy imports; `Decimal` from stdlib

### Backend API (`tailorings.py`)

- [x] Import: `LlmTriggerLog` → `LlmUsageLog`
- [x] `_get_tailoring_trigger_count`: added `'letter_regen'` to burst-limit event types (counts against hourly limit, not monthly quota)
- [x] `_cleanup_old_trigger_logs`: 30 days → 90 days; updated class reference and docstring
- [x] `regenerate_letter` endpoint: `event_type="tailoring_regen"` → `event_type="letter_regen"`
- [x] All `LlmTriggerLog(...)` → `LlmUsageLog(...)` (replace_all)

### Backend API (`experience.py`)

- [x] Import: `LlmTriggerLog` → `LlmUsageLog`
- [x] `trigger_process`: `event_type="experience_process"` → `event_type="resume_process"`
- [x] Added `_cleanup_old_usage_logs` helper (90-day TTL, same logic as `tailorings.py`)
- [x] `trigger_process`: added `background_tasks.add_task(_cleanup_old_usage_logs, db)` after commit

### Tests (`conftest.py`)

- [x] `make_llm_trigger_log` → `make_llm_usage_log` (factory renamed); old name kept as one-line alias for backward compat

### Planning docs

- [x] `planning/34-database-schema-review.md` — `llm_trigger_log` section replaced with full `llm_usage_logs` schema including new columns, event types, index, retention, and partner API future note
- [x] `planning/35-billing-data-model.md` — `LlmTriggerLog` → `LlmUsageLog` (replace_all); quota query updated to include `tailoring_regen`; `letter_regen` exclusion noted; partner API FK noted
- [x] `planning/private/pricing-model.md` — open item marked `[~]` (schema ready, instrumentation pending); class name updated

---

## Day 10 — Grafana Dashboard Updates

Fixed stale DB queries from schema migrations and added per-user usage dashboard.

### `observability/dashboards/generate.py`

- [x] Dashboard 04 (Per-Tailoring Debug): fixed stale `generation_duration_ms` → `generation_telemetry->>'duration_ms'` and `model` → `models->>'letter'` column refs (dropped in migration `d0e1f2a3b4c5`)
- [x] Dashboard 05 (User Activity): fixed stale `FROM experiences` query (table dropped) → `experience_sources` with `sync_status`-based success rate; expanded from 6 → 12 panels
  - Added: Pending Approval stat, Total Tailorings stat, Shared Tailorings stat
  - Added: LLM Events per Day by Type timeseries (4 series from `llm_usage_logs`)
  - Added: New Signups per Week (12w) timeseries
  - Added: Top 10 Users by Tailoring Count table
  - Added: Experience Sources by Type / Status bargauge
- [x] Dashboard 06 (Per-User Usage): new dashboard — user dropdown (email display / UUID value from DB), 12 panels
  - User Details table (email, status, is_admin, username_slug, profile_public)
  - Quota stats: Total Tailorings, Tailorings This Month (monthly quota metric), LLM Events Last Hour (burst meter — same query as rate limiter, same 8/10 thresholds), Active Claims, Shared Tailorings, Notion Connected
  - LLM Events Over Time (30d) timeseries by event type
  - Claims by Source Type bargauge
  - Experience Sources table (sync_status, last_synced_at, error_message)
  - Recent Tailorings table (job title/company, status, shared flag)
  - Recent LLM Events table (model, token counts, cost_usd — null until instrumentation ships)
- [x] All JSON regenerated via `make generate-dashboards` (12 files: 6 dashboards × 2 envs); `--check` clean

---

## Day 11 — tailoring_debug_logs improvements

- [x] Migration `f1a2b3c4d5e6`: payload JSON→JSONB, `user_id` nullable FK (SET NULL on user delete), backfill from tailorings, `(event_type, created_at)` index — ID changed from planned `a1b2c3d4e5f6` (collision with existing `make_uploaded_at_nullable` migration)
- [x] ORM: JSONB type, `user_id` nullable column with SET NULL FK
- [x] `_write_debug_log`: `user_id` keyword param; all ~14 call sites in `_finalize_tailoring` updated to pass `user_id=user_id`
- [x] Enrich `generation_complete` payload: `batch_count`, `batch_errors` from `generation_telemetry` (captured before db.close())
- [x] `_cleanup_old_debug_logs()` (90d TTL) — self-contained, called at end of `_finalize_tailoring`
- [x] Fix OPS.md stale field references: `generation_duration_ms` → `total_duration_ms`, removed `embedding_model` (not written), `chunk_batch_count` → `batch_count`, `error_count` → `batch_errors`; updated SQL to use JSONB `->>` operators
- [x] `planning/34-database-schema-review.md`: updated `tailoring_debug_logs` section (schema, index, retention, payload keys); cascade table updated (user_id SET NULL)

---

## Day 12 — Chunk analysis bug fixes

Two bugs identified from live chunk debug output:

- [x] **Bug 1 — wrong semantic_type on sectionless chunks**: job title line ("Software Engineer — Linear", pos:0) was classified `job_requirement` + `Partial` by the LLM; root cause is LLM misclassification of pre-section metadata. Fix: pre-classify all chunks where `!is_display_ready(chunk)` as `score=-1 / semantic_type="other" / include_in_scoring=False / should_render=False` before LLM dispatch — `is_display_ready()` is single source of truth; no LLM call wasted. Applied to both vector and LLM paths in `chunk_matcher.py`. Also saves 1–2 LLM calls per tailoring.
- [x] **Bug 2 — count/display mismatch**: summary strip showed 11 Partial but job posting view showed 10. Root cause: `AnalysisView` counted by `should_render !== false` but didn't check `display_ready`. Pos:0 had `display_ready=false` (hidden from view) but `should_render=true` (counted). Fix: added `c.display_ready !== false` to `scored` filter in both `AnalysisView` and `fitAnalysisToText`. Count now exactly mirrors posting view.
- [x] No schema changes — code-only fix in `chunk_matcher.py` + `AnalysisView.tsx`

---

## Day 13 — Resume Export (PDF)

Full implementation of `planning/37-resume-export.md`. Generates a tailored one-page resume PDF from an existing Tailoring using the same claim match intelligence, no LLM-invented content, Playwright PDF render.

### Migration (`e1f2a3b4c5d6`)

- [x] Added `tailorings.resume_draft JSONB nullable` — same pattern as `generation_telemetry`, `notion_export`

### Backend ORM (`database.py`)

- [x] `Tailoring`: added `resume_draft: Mapped[dict | None]` mapped column

### Backend schemas (`schemas/resume.py`) — NEW FILE

- [x] `EducationEntry`, `ResumeSection` (embeds group metadata at generation time), `ResumeContactOverride`, `ResumeDraft` (with `education_data: list[EducationEntry]`; `education_group_ids` kept as legacy empty list), `BulletPolishResult`, `BulletPolishResponse`

### Backend prompt (`prompts/resume_polish.py`) — NEW FILE

- [x] `PROMPT_NAME = "resume_bullet_polish"`, `TEMPERATURE = 0.2`, `SYSTEM` prompt with 5 hard constraints and style guide

### Backend selector service (`services/resume_selector.py`) — NEW FILE

- [x] `generate_resume_selection`: phase 1 mechanical selection — cosine similarity re-run against scored job chunks, section weight classification (`must_have`/`nice_to_have`/`unclassified`), YOE-only claim filtering, ungrouped work/edu from `group_key`, location lookup from `ExperienceSource.source_data.extracted`, GitHub claims → `group_type="repository"`, date sort descending
- [x] `check_resume_prerequisites`: warns on `no_resume_source`, blocks on `no_active_claims`
- [x] `_parse_date_for_sort`: "Mon YYYY" → sortable `(year, month)` tuple; Present/None → `(9999, 12)`
- [x] Deviation from plan: `education_group_ids` replaced with embedded `education_data: list[EducationEntry]` — `ExperienceGroup` rows are never created by the pipeline; ungrouped education claims use `group_key = "Degree | Institution"`

### Backend polisher service (`services/resume_polisher.py`) — NEW FILE

- [x] `polish_bullets`: `ThreadPoolExecutor(max_workers=4)`, one LLM call per bullet, returns `BulletPolishResponse`

### Backend renderer service (`services/resume_renderer.py`) — NEW FILE

- [x] `render_resume_html`: builds contact HTML as `markupsafe.Markup` (all values escaped; `# nosec B704` with block comment explaining false positive), triple email fallback (`communication_email` → `auth_identities[0].email` → `user.email`), section `group_type` embedded for template split
- [x] `render_resume_pdf`: Playwright headless print-to-PDF, Letter format, 0.5in margins
- [x] Deviation from plan: renderer takes `claims: dict[str, ExperienceClaim]` only (no `groups` or `education_groups` params — all metadata embedded in draft)

### Backend template (`templates/resume.html.j2`) — NEW FILE

- [x] Arial 10pt, left-aligned header, name 18pt bold, contact 9.5pt (email|tailord|linkedin|location), 12pt section titles, two-row entry layout (org+location row 1, title+dates row 2), `@media screen` 0.5in padding for iframe preview, `@media print` clean for Playwright
- [x] Jinja2 macro for DRY entry rendering; `selectattr`/`rejectattr` on `group_type` to split Experience vs Projects sections

### Backend API (`api/resume.py`) — NEW FILE; (`api/tailorings.py`) MODIFIED

- [x] 5 endpoints: `POST /generate`, `GET /`, `PATCH /`, `POST /polish`, `POST /pdf` + bonus `GET /html` for iframe preview
- [x] `tailorings.py` `get_tailoring`: added `"resume_draft": tailoring.resume_draft` to response for persistence across page reloads

### Backend main (`main.py`)

- [x] Registered `resume_router` with `include_router`

### Backend tests

- [x] `conftest.py`: added `make_claim`, `make_group` factories (265 total tests)
- [x] `tests/services/test_resume_selector.py` — NEW: 8 unit tests (section classify, YOE filter, prerequisites, date sort)
- [x] `tests/integration/test_resume.py` — NEW: 10 integration tests (generate, get, patch, pdf, warnings); 265 tests pass

### Frontend types (`types/index.ts`)

- [x] Added `EducationEntry`, `ResumeSection` (with embedded group metadata fields), `ResumeContactOverride`, `ResumeDraft` (with `education_data?: EducationEntry[]`), `ExperienceGroup`; `Tailoring` extended with `resume_draft?: ResumeDraft | null`

### Frontend API routes (5 new files)

- [x] `tailorings/[id]/resume/route.ts` — GET + PATCH proxy
- [x] `tailorings/[id]/resume/generate/route.ts` — POST proxy
- [x] `tailorings/[id]/resume/polish/route.ts` — POST proxy
- [x] `tailorings/[id]/resume/pdf/route.ts` — binary PDF proxy (raw fetch + `buildUserHeaders`)
- [x] `tailorings/[id]/resume/html/route.ts` — HTML proxy returning `text/html` for iframe

### Frontend proxy (`lib/proxy.ts`)

- [x] Extracted `buildUserHeaders(session)` helper — used by PDF and HTML routes

### Frontend components (2 new files, 1 modified)

- [x] `ResumeExportPanel.tsx` — Dialog `max-w-[92vw] w-[1140px]`, two-column layout (300px controls + flex-1 preview), iframe preview at `8.5in × 11in`, `previewKey` increments on PATCH to force iframe reload, pre-generation empty state
- [x] `ResumeDraftPreview.tsx` — section toggles, bullet toggles, AI polish with accept/revert, contact override inputs (with `htmlFor`/`id` for a11y), education entries from `education_data`, disclaimer note about Tailoring scoring referencing original resume
- [x] `TailoringDetail.tsx` — Resume button (FileText icon, same `textBtnCls` pattern), Dialog open/close pattern, `initialDraft` from `tailoring.resume_draft`

### Makefile

- [x] Bandit invocation: pipe through `grep -v "^\[tester\]\|^\[manager\]"` to suppress nosec false-positive noise; `exit ${PIPESTATUS[0]}` preserves exit code

### Planning (`planning/37-resume-export.md`) — NEW FILE

- [x] Full design doc; added "Refresh layout" future work item (re-derive structure while preserving user edits); added "Resume-scoped claim versions" north star (rescore Tailoring against generated resume for consistency)

### Results

- [x] 265 backend tests pass; 0 failures
- [x] Frontend: `tsc --noEmit` clean, ESLint clean, no type errors
