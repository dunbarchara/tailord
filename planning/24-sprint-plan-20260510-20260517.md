# Sprint Plan: 2026-05-10 – 2026-05-17

## Day 20 — Manual Entry, Thread Safety, ATS Client

- [x] Made `job_url` nullable in DB; added `raw_description` column (`job_url_nullable_raw_description` migration)
- [x] `POST /tailorings` accepts `company`, `title`, `description` as fallback when URL is absent or fails validation
- [x] Soft URL validation: surfaces warning and prompts user to fill in manual fields instead of hard-blocking
- [x] Updated `NewTailoringForm.tsx` with manual entry fields; URL validation failure shows inline prompt
- [x] Fixed thread-safety in chunk scorer: `_score_chunk_vector` now creates its own DB session (shared session unsafe across `ThreadPoolExecutor` threads); added 5-minute wall-clock timeout on chunk scoring
- [x] Added `backend/app/core/ats_client.py`: Greenhouse/Lever public API fetch as first-pass alternative to Playwright scraping (168 lines, 159 lines of tests)
- [x] Added `backend/app/core/extract.py`: validates scraped content contains recognizable job signals before proceeding (176 lines of tests)
- [x] Created `planning/23-experience-capture-north-star.md`: Passive Experience Capture north star — Instant Message Experience, Plugins, journal layer

---

## Day 21 — Editable Tailorings Workbench

### Theme
The Analysis view is the user's workbench. The platform does its best to parse job postings
correctly but cannot guarantee perfect output. The tailoring is ultimately the user's advocacy
artifact, so we surface controls to correct mistakes and express preferences.

### Toolbar Design Decision
Context-aware controls per view — only render controls applicable to the selected tab, rather
than always showing the full set greyed out.

Rationale:
- Analysis is a workbench — edit controls belong here
- Posting is a presentation layer derived from Analysis — no editing needed; greyed-out controls
  would imply editability where there is none
- Letter is a distinct writing artifact — it deserves its own controls (rephrase, tone) that share
  no overlap with Analysis controls
- Cognitive load: three distinct toolbars are clearer than one toolbar with conditional greying

### Analysis View — Refresh Modes

- [x] Implement **Regenerate** — pre-existing button retained in toolbar; exposed alongside new Refresh All and Edit controls (context-aware, analysis tab only)
- [x] Implement **Refresh All** — `POST /tailorings/{id}/refresh` endpoint + `refresh_job_chunks` in `chunk_matcher.py`; frontend: `tailorings/[id]/refresh/route.ts`, `handleRefreshAll` in `TailoringDetail.tsx`, RefreshCw icon button in toolbar; sets `enrichment_status=pending`, restarts chunks polling via `refreshKey`; skips `is_requirement=false` chunks

### Analysis View — Editor Mode

- [x] Add "Edit" button to Analysis toolbar that unlocks editor mode — Pencil icon button → `enterEditMode()`; toolbar swaps to "Save changes" + "Discard" while in edit mode; `editMode` prop threaded through `TailoringDetail` → `AnalysisView` → `JobPosting`
- [x] Toggle `isRequired` per chunk — `is_requirement` column (migration `9f8e7d6c5b4a`); PATCH endpoint; "Requirement / Not a req." button in `ChunkItem` edit controls
- [x] Toggle `shouldRender` per chunk — Eye/EyeOff "Shown / Hidden" button in `ChunkItem` edit controls; PATCH saves field on save
- [x] Inline textarea for chunk text — click-to-edit textarea in `ChunkItem`; saves on blur if content changed; scores preserved (no clearing)
- [x] Group assignment — section picker dropdown in `ChunkItem` with typeahead new-group input; mass-move prompt in `SectionBlock` (just this one / subsequent / all / cancel); `onSectionPending` → `handleSectionPending` → `pendingMassMove` state
- [x] Rename group — editable section header in `SectionBlock` (click to edit, blur to save); `POST /tailorings/{id}/chunks/rename-group` endpoint + route; "Ungroup" (2-tap confirm) removes section from all chunks
- [~] Reorder chunks within a group — position field is part of PATCH contract and save loop, but no drag/arrow UI built; deferred to next session
- [x] "Show hidden" toggle — `showHidden` state in `AnalysisView`; Eye/EyeOff button in sticky header strip (only visible in edit mode); `isHidden` prop dims chunks where `should_render=false`
- [x] Delete chunk — `DELETE /tailorings/{id}/chunks/{chunkId}` endpoint + `tailorings/[id]/chunks/[chunkId]/route.ts`; Trash2 button with 2-tap / 2.5 s confirm in `ChunkItem` edit controls
- [~] Merge chunks — `POST /tailorings/{id}/chunks/merge` endpoint + `tailorings/[id]/chunks/merge/route.ts` done; no selection UI built in `ChunkItem`; deferred
- [x] Add chunk manually — "Add chunk" textarea per section in `SectionBlock`; "Add section" input at bottom of posting list; `POST /tailorings/{id}/chunks` endpoint + route; tmp-id optimistic creation, canonicalised on save
- [x] Save behavior — `handleSaveChanges` in `TailoringDetail`: diffs draft vs original, fans out DELETE/POST/PATCH calls in parallel, refetches canonical state; `draftChunks` drives `displayChunksData` during edit mode
  - Score preservation: content edits no longer clear `match_score`/`match_rationale`/`advocacy_blurb`/`enriched_at`; scores survive cosmetic edits; user-triggered rescore is explicit
  - Added `scored_content TEXT NULL` to `job_chunks` (migration `a8b9c0d1e2f3`); populated at all three scoring sites in `chunk_matcher.py`; serialized in `_serialize_chunk`; typed in frontend `JobChunk`
  - Merge endpoint: scores also preserved after merge; `scored_content` null until next rescore

### Letter View Controls

- [~] Design edit surface for the generated letter — deferred out of scope for Day 21

### Profile Corrections Architecture

- [x] Profile updates stored under `"corrections"` key in `extracted_profile` — non-destructive, never overwrites extracted `"resume"` data; `_format_sourced_profile` merges corrections at read time
- [x] `yoe_override` field added to `ProfileUpdate` and `corrections` layer; generator reads it to override computed YoE
- [x] Overlap-aware YoE calculation in `tailoring_generator.py` — `_merge_intervals` merges overlapping role date spans before summing; replaces naive sum that double-counted concurrent roles

### Philosophy
The platform's parse is the starting point, not the final word. The tailoring belongs to the
candidate. Every control surfaces because we assume we might have gotten something wrong.

---

## Day 21 continued — Public Tailoring UX Polish

### Dynamic Candidate Footer
- [x] Backend: `get_public_tailoring` now returns `author_title`, `author_email`, `author_linkedin`, `author_profile_public` sourced live from the author's `extracted_profile.resume` — footer on shared pages is no longer baked into the generated markdown
- [x] `stripBriefFooter` extended to strip the trailing `---` hr as well as the `*Name · ...*` line from generated output; both views now get a clean content end
- [x] `CandidateFooter` extracted as a named component in `PublicTailoringView.tsx` — reused for both letter and posting tabs with a consistent explicit `<hr>` separator above it

### Author Profile Link
- [x] `TailoringHeader` gains optional `authorUrl` prop — when provided, renders author name as a `<a>` link with a `→` arrow; callers in private context (AdvocacyLetter, JobPosting) unchanged
- [x] Public tailoring `page.tsx` passes `authorUrl` when `author_profile_public && author_slug`; also passes to `CandidateFooter` for the contact line name link
- [x] Bottom platform footer simplified to "Generated with Tailord" only — author name and sources moved into `CandidateFooter` above the divider

### Sources Placement
- [x] Sources row (Resume + GitHub repos) moved from platform footer into `CandidateFooter`, below the candidate contact line — sits inside the content area, not alongside the Tailord branding

### Collapsible Inferred Profile
- [x] Inferred Profile section in `ExperienceManager` collapsed by default (`profileExpanded = false`); click-to-expand toggle with `ChevronDown`/`ChevronUp` and context-sensitive subtext

### Back Navigation Fix
- [x] `TailoringHeader` author link converted from `<a href>` to Next.js `<Link>` — plain `<a>` caused a full page navigation; on back, Next.js intercepted `popstate` and attempted a soft RSC re-render that stalled in dev (self-referential fetch), leaving React in a pending transition with effects never running and the tab switcher dead
- [x] Letter prose links in `ReactMarkdown` given `target="_blank"` via `components.a` — all prose links are external (job posting, LinkedIn); eliminates the same back-nav path for those links
- [x] `export const dynamic = 'force-dynamic'` added to public tailoring `page.tsx` — fixes incorrect "Static" classification caused by Next.js 15+ no longer treating `cache: 'no-store'` as a dynamic opt-in

---

## Day 23 — Observability Layer 1 (Structured Logging + Correlation IDs)

Reference architecture: `planning/25-observability-strategy.md`

### Layer 1 — Foundations (no new infra)
- [x] Added `structlog>=25.0.0` to `backend/pyproject.toml`
- [x] Created `backend/app/middleware/correlation.py` — pure-ASGI `CorrelationIdMiddleware`: generates UUID4 per request, calls `structlog.contextvars.clear_contextvars()` + `bind_contextvars(correlation_id=...)`, echoes `X-Correlation-Id` response header; safe for SSE streaming (no `BaseHTTPMiddleware`)
- [x] Rewrote `backend/app/logging.py` — structlog configured with `ProcessorFormatter`; native `structlog.get_logger()` loggers and stdlib `logging.getLogger()` loggers both emit JSON via the same pipeline; `merge_contextvars` injects `correlation_id` automatically into every record
- [x] Updated `backend/app/main.py` — registers `CorrelationIdMiddleware` (outermost) + `_RequestLoggingMiddleware` (logs `request_start` / `request_complete` events with method, path, status_code, duration_ms); uses `structlog.get_logger()`
- [x] Updated `backend/app/core/llm_utils.py` — migrated to `structlog.get_logger()`; all log calls use native structlog keyword args (`logger.info("llm_call_complete", model=model, latency_ms=ms, ...)`); no `extra={}` wrapper
- [x] Updated `backend/app/api/tailorings.py` — migrated to `structlog.get_logger()`; `_finalize_tailoring` accepts `correlation_id: str = ""`, calls `structlog.contextvars.bind_contextvars(correlation_id=..., tailoring_id=...)` at task entry so both fields appear in every subsequent log line automatically; added `_write_debug_log()` non-fatal helper; per-phase wall-clock timing; `TailoringDebugLog` now fully populated: `generation_started`, `phase_complete`/`phase_error` for all 5 phases, `generation_complete`/`generation_error`; correlation_id captured at `background_tasks.add_task()` call time via `structlog.contextvars.get_contextvars()`
- [x] Updated `frontend/src/lib/proxy.ts` — all 3 proxy functions (`proxyToBackend`, `proxyToBackendWithUser`, `proxyStreamToBackendWithUser`) generate a UUID, send `X-Correlation-Id` to backend, include `correlation_id` in log calls, echo in response headers

### Layer 2 — Metrics (requires Prometheus infra)
- [ ] Add `prometheus_client` to `backend/pyproject.toml`
- [ ] Create `backend/app/metrics.py` — define all counters, histograms, gauges (HTTP layer, LLM layer, tailoring pipeline, experience processing)
- [ ] Instrument HTTP layer via request/response middleware (request count, latency histogram)
- [ ] Instrument LLM calls in `llm_utils.py` (emit metrics alongside structured logs)
- [ ] Instrument tailoring pipeline phases (phase duration histograms, `tailoring_active_generations` gauge)
- [ ] Expose `/metrics` endpoint in `main.py` (internal only)
- [ ] Add Prometheus + Grafana to `backend/docker-compose.yml` for local dev
- [ ] Provision Azure Managed Prometheus + Azure Managed Grafana in `infra/providers/azure/monitoring.tf`

### Layer 3 — Dashboards and Alerting
- [ ] Platform Health dashboard (Prometheus + Log Analytics)
- [ ] LLM Observability dashboard
- [ ] Tailoring Pipeline dashboard
- [ ] Per-Tailoring Debug dashboard (Log Analytics KQL + Postgres for TailoringDebugLog)
- [ ] Alert rules in Terraform (8 alerts defined in strategy doc)
- [ ] `azurerm_monitor_action_group` with email notification channel

### Layer 4 — Distributed Tracing (optional)
- [ ] `opentelemetry-sdk` + FastAPI/SQLAlchemy auto-instrumentation
- [ ] Provision `azurerm_application_insights` in Terraform
- [ ] Manual span instrumentation in `_finalize_tailoring` (per-phase spans)
- [ ] Next.js `instrumentation.ts` with OTel SDK

---

## Backlog

- [ ] LinkedIn URL missing protocol — stored values like `linkedin.com/in/username` (no `https://`) render as relative paths in `CandidateFooter` (`PublicTailoringView.tsx`), producing broken URLs like `localhost:3000/u/slug/linkedin.com/...`. Two-part fix: (1) normalize `authorLinkedin` in `CandidateFooter` with the same `startsWith('http') ? url : \`https://\${url}\`` guard already used in `ProfileSidebar` and `ParsedProfile`; (2) strip leading protocol/www at extraction time in `profile_extractor.py` so values are stored consistently, then always prepend `https://` at render.
- [x] Editable job title and company name — `PATCH /tailorings/{id}` endpoint added (`TailoringJobUpdate` schema, patches `job.extracted_job`); PATCH handler added to `tailorings/[id]/route.ts`; `TailoringDetail.tsx` enters edit mode with `jobDraft` state; toolbar shows live preview (read-only display of draft values); `JobPropertiesPanel` in `AnalysisView` right panel contains editable Position + Company inputs in edit mode; `jobDraft`/`onJobDraftChange` props threaded `TailoringDetail` → `AnalysisView`; saved alongside chunk changes in `handleSaveChanges`
- [x] Edit mode chunk text always rendered as textarea — removed click-to-edit pattern in `ChunkItem`; textarea always visible when `editMode` is true; `useEffect` syncs `draftContent` when `chunk.content` changes externally

---

## Day 22 — Contact Fields + Letter Decoupling

### Inferred Profile — Contact Fields
- [x] Added `email`, `phone`, `linkedin` to `ProfileCorrections` interface (`types/index.ts`) — backend `ProfileUpdate` already supported these fields
- [x] Wired contact fields into `ExperienceManager.tsx` state (`profileFields`), `syncProfileFromRecord`, and `handleProfileSave`
- [x] Redesigned Inferred Profile layout: Row 1 (3-col): YoE · Title · Location; Row 2 (full-width): Headline; Row 3 (full-width): Summary; Row 4 (3-col): Email · Phone · LinkedIn

### Advocacy Letter Decoupling
- [x] Added `letter_content JSONB NULL` column to `tailorings` — migration `b1c2d3e4f5a6`; `Tailoring` ORM model updated
- [x] `generate_tailoring()` in `tailoring_generator.py` now returns `tuple[str, dict]` — `(rendered_markdown, content.model_dump())`; `generated_output` preserved for Notion export compat
- [x] `_finalize_tailoring` in `tailorings.py` unpacks tuple, stores `letter_content` alongside `generated_output`
- [x] Private GET `/tailorings/{id}` now returns `letter_content` + `author_email`, `author_title`, `author_linkedin` sourced live from the user's experience profile — no stale baked-in contact data
- [x] Public GET `/tailorings/public/{slug}` now returns `letter_content` alongside existing fields
- [x] `Tailoring` TS interface updated: added `letter_content`, `author_email`, `author_title`, `author_linkedin`; added `AdvocacyStatement` and `LetterContent` types
- [x] `AdvocacyLetter.tsx` rewritten — `StructuredLetter` component renders greeting, opening, per-statement body + source tags, closing, contact line, and brief footer all deterministically from live data; falls back to `generated_output` markdown for pre-migration records
- [x] `PublicTailoringView.tsx` updated — `StructuredLetterBody` renders structured letter when `letterContent` prop present; `stripBriefFooter` kept as fallback for old records; `CandidateFooter` unchanged (still renders sources + contact below the letter)
- [x] Public tailoring `page.tsx` passes `letter_content` to `PublicTailoringView`

### Inferred Profile — UX Polish
- [x] Correctable fields extended: added `email`, `phone`, `linkedin` to `correctable` tuple in both the re-upload preservation path and the PATCH path in `experience.py`
- [x] YoE field placeholder now shows auto-computed value — `computeYoE()` helper ported from backend overlap-aware logic to TypeScript, runs in-component against `resume.work_experience`; placeholder renders e.g. `6 (auto-computed)` when field is empty; fixed edge case: `yoe >= 0` (was `yoe > 0`) so `0 (auto-computed)` renders correctly
- [x] Save signals button disabled by default — `profileFieldsInitial` state tracks last-saved baseline; button enabled only when `profileFields !== profileFieldsInitial`; baseline reset after successful save
- [x] Fix clear-and-save: profile correction fields now send `""` (not `null`) when cleared — `null` removed the correction key causing fallback to extracted value on reload; `""` stores an explicit empty override in corrections; backend `v is not None` filter already allows `""` through to both corrections and resume layers

### Summary Chunk Cleanup
- [x] Removed resume summary chunk creation from `experience_chunker.py` — summary is always present in `_format_sourced_profile` baseline context; embedding it competed with specific bullets in top-K cosine retrieval without adding signal
- [x] Removed GitHub readme_summary chunk creation from `_github_repo_chunks` — same rationale; `experience_claims` and `detected_stack` chunks retained as they carry specific, non-redundant signal
